# Changelog

The authoritative changelog lives at the repo root:

- `CHANGELOG.md` (at the repository root) — release notes, Keep-a-Changelog
  format, Semantic Versioning.

If you are browsing the rendered docs site, follow the link above.
If you are reading `docs/` on disk, open `../CHANGELOG.md` from the
repository root. We keep a single source of truth to avoid the two
files drifting out of sync.
