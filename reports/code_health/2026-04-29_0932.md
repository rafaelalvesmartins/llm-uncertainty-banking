# Code Health Scan -- 2026-04-29 09:32 local

**Status:** 🟠 RECOMMENDED

**Summary:** 1 P1, 3 P2, 3 P3 findings — see "Top 5 next moves".

---

## 1. Disk integrity

`scripts/check_integrity.py` exits 0 (gating step passes). The script reports one FAIL (`src/lub/agents/adapters/langgraph.py` "unterminated string literal at line 93"). Cross-checking via the Read tool: the file is intact end-to-end (95 lines, string terminated, proper EOF). This is the documented Windows-mount sync-lag artifact, NOT a code finding (see "Tool issues").

## 2. AST sweep + Python 3.11+ usage

No genuine AST parse failures. `tomllib` remains imported in `cli/benchmark.py`, `reports/crosswalk.py`, `reports/mapping.py`; `pyproject.toml` declares `python_requires>=3.11`, so this is intentional. No findings.

## 3. Architectural drift

Five subpackages remain absent from `docs/architecture.md`: **`challenge`**, **`cnh`**, **`governance`**, **`integrations`**, **`mcp`**. The only mention of "MCP" in architecture.md (lines 271–272) refers to ruflo's auto-generated MCP surface, not `lub.mcp`. The four import-linter contracts in `pyproject.toml` are present and unchanged. `git log --since="24 hours ago"` returned no commits. `git status --short` shows 305 modified paths sibling to this repo (under `00_Meta/`, `02_Evidencias_Profissionais/`, `.claude/`); none touch `src/lub` or `tests/`. No commit performed.

- **P2** | `docs/architecture.md` | five subpackages (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) are not described in the layered overview | add a short paragraph per subpackage placing each in L1–L5 (or trust/orchestration crosscut). **(unchanged)**

## 4. Code-smell + duplication

- **P2** | `src/lub/uncertainty/semantic_entropy.py:91` | `a.strip().lower() == b.strip().lower()` duplicates the canonical normalizer outside `_text_utils.py` | replace with `normalize(a) == normalize(b)` from `lub._text_utils`. **(unchanged)**

Duplication searches clean: no inline `entropy = -sum(p * math.log(p)` outside `_math_utils.py`; no `except Exception: pass`; no bare `except BaseException`; no f-string/%-format/`.format()` SQL injection patterns; `_conn` accesses are all annotated `# noqa: SLF001` (first-party extension, acceptable). No findings on those axes.

- **P3** | `src/lub/dashboard/render.py:88` | chart.js loaded from CDN with `crossorigin` but no SRI hash; in-file TODO documents the recipe | run the recipe and add `integrity="sha384-..."` to the `<script>` tag. **(unchanged)**

## 5. Backend capability + estimator coverage

Backends (`src/lub/wrappers/*.py`) declare:
- `dummy`, `hf`: GENERATE | LOGPROBS | EMBED
- `vllm`: GENERATE | LOGPROBS
- `openai`: GENERATE | EMBED (no LOGPROBS)
- `anthropic`: GENERATE only

Estimators with `REQUIRES_CAPABILITIES`: `adaptive_conformal`, `conformal`, `conformal_sampling` (LOGPROBS), `graph_laplacian`, `mahalanobis` (EMBED), `mondrian_conformal` (LOGPROBS).

- **P1** | `src/lub/uncertainty/{perplexity,token_logprob,p_true,semantic_entropy,sar,sentence_sar}.py` | six estimators that consume token-level logprobs do not declare `REQUIRES_CAPABILITIES`, so they inherit the base default (GENERATE only) and silently fall through on `openai`/`anthropic` instead of failing fast at pipeline construction | add `REQUIRES_CAPABILITIES = BackendCapability.GENERATE | BackendCapability.LOGPROBS` to each class. **(unchanged)**

## 6. Test coverage by subpackage

By "tests importing the subpackage" count (more accurate than file-name match):

| Subpackage | src files | importing tests | ratio |
|---|---:|---:|---:|
| cnh | 3 | 0 | 0% |
| integrations | 2 | 0 | 0% |
| dashboard | 7 | 0 | 0% |
| compliance | 8 | 1 | 12% |
| mcp | 9 | 5 | 56% |

`mcp` crossed the 50% threshold (was 0% in 2026-04-29_0442.md; `test_mcp_airmf.py`, `test_mcp_plugin_loader.py`, `test_mcp_ruflo_compat.py` plus 2 more importers landed). `cnh`, `integrations`, `dashboard` remain at 0%. `compliance` at 12% is also under threshold.

- **P2** | `tests/unit/{cnh,integrations,dashboard}` | three subpackages with zero tests importing them; `compliance` at 1/8 = 12% also below threshold | add at minimum a smoke-import test (`from lub.<sub> import …`) per module. Highest urgency for `dashboard` (7 source files; rendering bugs are user-visible). **(changed — `mcp` resolved; `dashboard` and `compliance` newly surfaced)**

## 7. Recent-activity recheck (last 24h)

No commits in last 24h; no `src/lub` or `tests/` working-tree modifications in this repo. Re-running `check_integrity.py` and AST parse on the working tree as a whole: same single sync-lag FAIL on `langgraph.py` (cross-checked: intact via Read tool). Nothing to flag.

## Top 5 next moves

1. **Declare `REQUIRES_CAPABILITIES` on the 6 LOGPROBS-dependent estimators** (`perplexity`, `token_logprob`, `p_true`, `semantic_entropy`, `sar`, `sentence_sar`) so `openai`/`anthropic` backends fail fast at pipeline construction. **(unchanged)**
2. **Replace `a.strip().lower() == b.strip().lower()` at `src/lub/uncertainty/semantic_entropy.py:91`** with `normalize(a) == normalize(b)` from `lub._text_utils`. One-line change. **(unchanged)**
3. **Document the 5 missing subpackages** (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) in `docs/architecture.md`. **(unchanged)**
4. **Add smoke-import tests for `dashboard` (0/7), `cnh` (0/3), `integrations` (0/2)** — `mcp` is now at 56% and out of the spotlight; `dashboard` is the most user-visible new gap. **(changed — `mcp` resolved; `dashboard` and `compliance` are newly under-covered)**
5. **Pin `chart.js@4.4.1` with SRI in `dashboard/render.py:88`** per the in-file recipe. **(unchanged)**

---

*Compared to previous report (`2026-04-29_0442.md`):*
- Same: items 1, 2, 3, 5 (REQUIRES_CAPABILITIES, semantic_entropy strip().lower(), architecture.md gap, chart.js SRI).
- Resolved: `mcp` is no longer zero-coverage — `test_mcp_airmf.py`, `test_mcp_plugin_loader.py`, `test_mcp_ruflo_compat.py`, plus two additional importing tests, bring the ratio to 56%.
- New: `dashboard` (0/7) and `compliance` (1/8 = 12%) surface as below-threshold; `dashboard` joins the Top 5 in item 4.

## Tool issues

- Bash mount returned a truncated snapshot of `src/lub/agents/adapters/langgraph.py` (92 lines vs. 95 via Read tool); `check_integrity.py` and `ast.parse` flagged the truncated bytes as `SyntaxError: unterminated string literal at line 93`. Read-tool view confirms the file is intact and parseable. Per heuristic, NOT flagged as a code finding.

---

*Slack one-liner:* 1 P1 + 3 P2 open; `mcp` zero-coverage finding resolved (now 56%); `dashboard` (0/7 tests) and `compliance` (1/8) surfaced as the new under-covered subpackages.
