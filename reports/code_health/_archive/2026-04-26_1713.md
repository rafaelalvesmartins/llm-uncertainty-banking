# Code Health Scan — 2026-04-26 17:13 (local)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 5 P1 findings (disk-corruption / truncated source files). Steps 2–7 skipped per gating rule — see "Top 5 next moves".

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1**. Five source files
under `src/lub/` failed AST/syntax parsing — each is truncated mid-statement,
matching the recurring Windows + bash-mount disk-corruption signature. No
repairs were attempted (read-only scan).

Flagged files (with the parser's reported failure point):

- **P1** | `src/lub/dashboard/in_memory_source.py:123` | Unterminated string literal — file ends mid-dict-literal at 122 lines / 4708 bytes (`out.append({ "i…`). | Restore from git (`git checkout -- src/lub/dashboard/in_memory_source.py`) or from the last good backup.
- **P1** | `src/lub/dashboard/ledger_source.py:134` | `'(' was never closed` — file truncates inside a generator expression on the ECE bucket loop (133 lines / 5278 bytes, ends `mean_conf = sum(c for `). | Restore from git; rerun `check_integrity.py` to confirm.
- **P1** | `src/lub/dashboard/server.py:173` | `'(' was never closed` — file ends inside the `def run_uvicorn(` signature (173 lines / 5734 bytes). | Restore from git; rerun `check_integrity.py`.
- **P1** | `src/lub/evidence/protocols.py:133` | Unterminated triple-quoted string — file ends inside the docstring of `record_drift_event` (132 lines / 4174 bytes). | Restore from git; rerun `check_integrity.py`.
- **P1** | `src/lub/reports/crosswalk.py:159` | `'[' was never closed` — file ends inside the `__all__` list (159 lines / 5104 bytes, last line `"ControlMapping"`). | Restore from git; rerun `check_integrity.py`.

All five truncations are at end-of-file, which is the canonical fingerprint
of the known mount-layer corruption rather than authored bugs.

## 2. AST sweep + Python 3.11+ usage

Skipped — disk integrity gate failed.

## 3. Architectural drift

Skipped — disk integrity gate failed.

## 4. Code-smell + duplication

Skipped — disk integrity gate failed.

## 5. Backend capability + estimator coverage

Skipped — disk integrity gate failed.

## 6. Test coverage by subpackage

Skipped — disk integrity gate failed.

## 7. Recent-activity recheck (last 24h)

Skipped — disk integrity gate failed.

## Top 5 next moves

1. Restore the five truncated files listed above from git (they are tracked) — `git checkout -- src/lub/dashboard/in_memory_source.py src/lub/dashboard/ledger_source.py src/lub/dashboard/server.py src/lub/evidence/protocols.py src/lub/reports/crosswalk.py`.
2. Re-run `python scripts/check_integrity.py` and confirm exit 0 before any further work in those modules.
3. After restore, re-trigger this scheduled scan so steps 2–7 (AST sweep, architectural drift, code-smell, capability matrix, test coverage) actually execute and produce a real baseline.
4. Two of the three corrupted files live in `src/lub/dashboard/` — that subpackage may be hot in current edits; consider snapshotting it (or copying to a non-mounted path) before the next long edit session to limit blast radius the next time the mount layer truncates.
5. If corruption keeps recurring at end-of-file boundaries, add a post-write verification step to whatever tool is producing these writes (e.g. `python -c "import ast; ast.parse(open(p).read())"` after each save) — `scripts/check_integrity.py` already does this batch-mode and could be wired into a pre-commit hook.

---

*Compared to previous report (first run):*
- Same: n/a
- Resolved: n/a
- New: 5 P1 truncated-file findings.

*Tool issues:* none. Git is available; uncommitted changes exist across the parent worktree but none of the five corrupted files are among them (they are clean per `git status`), confirming this is filesystem corruption rather than in-flight edits — `git checkout` will restore them losslessly.

---

🔴 Code-health scan halted at the integrity gate: 5 source files under `src/lub/` are truncated and need a `git checkout` restore before the next scan can run.
