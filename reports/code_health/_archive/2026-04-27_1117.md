# Code Health Scan — 2026-04-27 11:17 (UTC)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 7 P1 findings — same seven corrupt source files as the previous cycle (`2026-04-27_1016.md`); none recovered, none new. Steps 2–7 skipped per the gating rule.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1** after scanning **342** `.py` files across 2 roots (one more file than the prior cycle's 341 — unrelated to the corruption set). All seven flagged paths are unchanged from the previous report; mtimes have not advanced, so no in-place restore was attempted in the last hour.

- **P1** | `src/lub/agents/__init__.py:1` | `SyntaxError: unterminated triple-quoted string literal (detected at line 41) at line 1`. File is 40 lines / 1,505 bytes; mtime `2026-04-27 05:08:03` (unchanged from previous cycle, ~6 h ago). Canonical blob at `90e281b` is 56 lines / 1,505 bytes — byte-count-equal, content-differs, the canonical Windows + bash-mount partial-write failure mode. | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py > /tmp/agents_init.py && cp /tmp/agents_init.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py`, then verify `wc -l` reads 56 and re-run `python scripts/check_integrity.py`.
- **P1** | `src/lub/agents/adapters/langgraph.py:77` | `SyntaxError: unterminated triple-quoted string literal (detected at line 82) at line 77`. File is 81 lines / 2,723 bytes; mtime `2026-04-27 09:10:35` (unchanged — **15th consecutive cycle flagged**, including the failed in-place restore at 09:10 UTC). Canonical 82 lines / 2,723 bytes. | From `C:\code\eb2niw`: write to `/tmp/langgraph.py` first via `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py > /tmp/langgraph.py`, then `cp /tmp/langgraph.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py`, then verify line count is 82. Plain `git show … > <repo path>` is empirically unsafe on this mount.
- **P1** | `src/lub/compliance/frameworks/bcb_4893.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. File is 18 lines / 550 bytes; mtime `2026-04-26 13:48:42` (~22 h ago, unchanged). Canonical 20 lines / 550 bytes. Same byte-equal/content-differs signature. | Same temp-then-`cp` recovery from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/bcbs_239.py:24` | `SyntaxError: unterminated triple-quoted string literal (detected at line 26) at line 24`. File is 25 lines / 797 bytes; mtime `2026-04-26 13:48:37` (unchanged). Canonical 27 lines / 797 bytes. | Same temp-then-`cp` recovery from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/eu_ai_act.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. File is 18 lines / 541 bytes; mtime `2026-04-26 13:48:31` (unchanged). Canonical 20 lines / 541 bytes. | Same temp-then-`cp` recovery from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/iso_23894.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. File is 18 lines / 545 bytes; mtime `2026-04-26 13:48:47` (unchanged). Canonical 20 lines / 545 bytes. | Same temp-then-`cp` recovery from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/sr_11_7.py:30` | `SyntaxError: unterminated triple-quoted string literal (detected at line 32) at line 30`. File is 31 lines / 1,119 bytes; mtime `2026-04-26 13:48:20` (unchanged). Canonical 32 lines / 1,119 bytes. | Same temp-then-`cp` recovery from `90e281b`.

`git log --since="24 hours ago" --oneline` (run from `C:\code\eb2niw`) returns the same four commits as recent cycles: `f91faf0 dfas`, `4c4805e Merge branch 'main' …`, `90e281b Atualização do workspace`, `f5e0bb3 Update workspace files`. `HEAD` is unchanged at `f91faf0`. All seven canonical blobs were re-verified recoverable from `90e281b` this cycle (line counts 56 / 82 / 20 / 27 / 20 / 20 / 32 match the originals).

`git status --short` continues to fail: `fatal: unknown index entry format 0x3a860000` (the high-byte signature drifts between cycles — `0x70680000` last hour, `0x3a860000` this hour — but `git log` and `git show` against committed blobs continue to work, so the recovery path is intact).

The `llm-uncertainty-banking` subdirectory is **not** its own git repo (no nested `.git`); all `git show` / `git log` recovery has to be run from `C:\code\eb2niw`.

## 2. AST sweep + Python 3.11+ usage

Skipped — gated by step 1.

## 3. Architectural drift

Skipped — gated by step 1.

## 4. Code-smell + duplication

Skipped — gated by step 1.

## 5. Backend capability + estimator coverage

Skipped — gated by step 1.

## 6. Test coverage by subpackage

Skipped — gated by step 1.

## 7. Recent-activity recheck (last 24h)

Skipped — gated by step 1. (For reference: no source files under `src/lub/` were modified in the last hour; the seven flagged files have unchanged mtimes since the previous cycle.)

## Top 5 next moves

1. **Restore `src/lub/agents/adapters/langgraph.py` from `90e281b`** using the temp-file-then-`cp` pattern (15th consecutive cycle flagged) (unchanged).
2. **Restore `src/lub/agents/__init__.py` from `90e281b`** with the same recovery pattern (unchanged).
3. **Restore the five `src/lub/compliance/frameworks/*.py` files** (`bcb_4893.py`, `bcbs_239.py`, `eu_ai_act.py`, `iso_23894.py`, `sr_11_7.py`) from `90e281b` — they share root cause and recovery steps (unchanged).
4. **Re-run `python scripts/check_integrity.py`** after each restore; do not batch all seven without re-verifying, since prior in-place restores have themselves been corrupted by the mount.
5. **Investigate the corrupt git index** (`fatal: unknown index entry format 0x3a860000`) — the changing high-byte signature each cycle suggests live corruption in `.git/index`; running `git read-tree HEAD` from `C:\code\eb2niw` would rebuild the index without touching working-tree files (unchanged).

---

*Compared to previous report (`2026-04-27_1016.md`):*
- Same: all 7 P1 findings persist with identical sizes, line counts, and mtimes; Top 5 unchanged.
- Resolved: none.
- New: none. The integrity scanner now reports 342 `.py` files vs. 341 last cycle — one new file added somewhere outside the corrupt set, but step 2 (AST sweep) is gated so the new file is not identified here.

Tool issues: `git status --short` still unusable due to corrupt index; report compiled without it. No file read failures encountered this cycle.

Disk-corruption gate failed for the 16th consecutive hourly scan — seven Python source files remain syntactically broken; restore them from `90e281b` via the temp-file-then-`cp` pattern before the next code-health scan.
