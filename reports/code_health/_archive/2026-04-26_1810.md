# Code Health Scan — 2026-04-26 18:10 (UTC)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 4 P1 findings (disk-corruption / truncated source files). Steps 2–7 skipped per gating rule — see "Top 5 next moves".

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1** after scanning 339 `.py` files across 2 roots. Four source files under `src/lub/` failed AST/syntax parsing — each truncates mid-statement at end-of-file, the canonical fingerprint of the recurring Windows + bash-mount corruption. No repairs were attempted (read-only scan).

Flagged files (with parser-reported failure point and last bytes):

- **P1** | `src/lub/dashboard/in_memory_source.py:123` | Unterminated string literal — file ends at 122 lines / 4708 bytes mid dict-literal (`out.append({ "i…`). | **Untracked in git** — restore from a backup or regenerate; `git checkout` will NOT work.
- **P1** | `src/lub/dashboard/ledger_source.py:134` | `'(' was never closed` — truncates at 133 lines / 5278 bytes inside an ECE-bucket generator (`mean_conf = sum(c for `). | **Untracked in git** — restore from backup / regenerate.
- **P1** | `src/lub/dashboard/server.py:173` | `'(' was never closed` — truncates at 173 lines / 5734 bytes inside the `def run_uvicorn(` signature. | **Untracked in git** — restore from backup / regenerate.
- **P1** | `src/lub/evidence/protocols.py:133` | Unterminated triple-quoted string — truncates at 132 lines / 4174 bytes inside `record_drift_event` docstring. | **Untracked in git** — restore from backup / regenerate.

Important correction vs. previous report: **all four files are currently untracked** (`?? src/lub/dashboard/`, `?? src/lub/evidence/protocols.py` per `git status --short`). The previous report (2026-04-26_1713.md) recommended `git checkout --` to restore them, which will silently no-op for untracked paths. They must be reconstructed from another source (editor/IDE backup, an unmounted copy, or regeneration from the spec/protocol they implement).

The fifth file flagged in the previous run, `src/lub/reports/crosswalk.py`, is no longer flagged — it now parses cleanly (resolved).

## 2. AST sweep + Python 3.11+ usage

Skipped — disk integrity gate failed.

## 3. Architectural drift

Skipped — disk integrity gate failed.

(Light context only, not a full step-3 run: `git log --since="24 hours ago" --oneline` shows recent activity at the parent-worktree level — top commits include `b97b746 do it all`, `8de41f3 Atualização`, `b23647f fix: restore 50 Unipampa ementas PDFs deleted by reorganization`, `6d35450 chore: resolve settings.local.json merge conflict`, `67d2907 docs: update STATUS_Cobertura.md — 732 tests, 93% coverage, 86 source files`. None touch the four corrupted files, consistent with corruption-not-edits.)

## 4. Code-smell + duplication

Skipped — disk integrity gate failed.

## 5. Backend capability + estimator coverage

Skipped — disk integrity gate failed.

## 6. Test coverage by subpackage

Skipped — disk integrity gate failed.

## 7. Recent-activity recheck (last 24h)

Skipped — disk integrity gate failed.

## Top 5 next moves

1. **(corrected)** Reconstruct the four truncated files — they are **untracked**, so `git checkout` is a no-op. Sources of truth, in order of preference: (a) IDE local-history / `.idea/shelf` or VSCode timeline; (b) a fresh copy from a non-mounted path; (c) regenerate from the protocol/spec each implements (`evidence/protocols.py` ↔ `lub.evidence` API; dashboard sources ↔ `LedgerSummary` protocol on `lub.ledger`).
2. Re-run `python scripts/check_integrity.py` and confirm exit 0 before the next scheduled scan; only then will steps 2–7 produce a real baseline. **(unchanged from prior report)**
3. After restoration, `git add` the four files (they should be tracked — losing them to mount corruption with no git copy is exactly the failure mode that bit us here). Add a one-line invariant to `CONTRIBUTING.md`: "every `src/lub/**.py` must be tracked before being trusted."
4. Three of the four corrupted files live in `src/lub/dashboard/` — that whole subpackage is untracked AND hot-edit-prone, which is the worst combination for the mount bug. Either commit it now or move active dashboard work to an unmounted scratch path until the mount layer is replaced. **(unchanged from prior report, escalated)**
5. Wire `scripts/check_integrity.py` into a `pre-commit` hook and a post-write step on whatever tool produces these writes (e.g. `python -c "import ast; ast.parse(open(p).read())"`). Recurring end-of-file truncations across two consecutive scans is a pattern worth automating against. **(unchanged from prior report)**

---

*Compared to previous report (2026-04-26_1713.md):*
- Same: 4 of 5 P1 truncations persist (`dashboard/in_memory_source.py`, `dashboard/ledger_source.py`, `dashboard/server.py`, `evidence/protocols.py`) — same line numbers, same byte counts, same failure signatures (no edits since).
- Resolved: `src/lub/reports/crosswalk.py` — no longer flagged by `check_integrity.py`.
- New: corrected diagnosis — the four remaining corrupted files are **untracked in git**, so the prior report's `git checkout` remediation is incorrect. Top-5 item #1 has been rewritten accordingly.

*Tool issues:* none. `git`, `python3`, and `scripts/check_integrity.py` all ran cleanly. Working tree has many uncommitted changes across the parent worktree (sample in `git status --short` truncated by tool reminder); none touch the four corrupted files.

---

🔴 Code-health scan halted at the integrity gate again — 4 of yesterday's 5 truncated files persist and (newly diagnosed) are untracked, so they must be reconstructed from a backup, not `git checkout`-ed.
