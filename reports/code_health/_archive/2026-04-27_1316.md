# Code Health Scan — 2026-04-27 13:16 (UTC)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 9 P1 findings — corruption set grew from 7 to 9 since the prior cycle (`2026-04-27_1216.md`); two new flags in `lub.compliance.__init__` and `lub.domains.__init__`. Steps 2–7 skipped per the gating rule.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1** after scanning **342** `.py` files across 2 roots (file count unchanged from prior cycle; flagged count increased from 7 → 9). All nine flagged paths share the canonical Windows + bash-mount partial-write signature: **byte-count on disk equals canonical blob byte-count, but on-disk line count is shorter** (truncation followed by NUL-padding to the original size). All nine canonical blobs were re-verified recoverable from `90e281b` this cycle.

- **P1** | `src/lub/compliance/__init__.py:3` | **NEW THIS CYCLE.** `SyntaxError: unterminated triple-quoted string literal (detected at line 14) at line 3`. On-disk 13 lines / 621 bytes; mtime `2026-04-26 13:51:49` (mtime is older than the prior cycle’s mtime — content drifted without an mtime update, the canonical late-stage symptom). Canonical blob at `90e281b` is 18 lines / 621 bytes — byte-equal, content-differs. | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/__init__.py > /tmp/compliance_init.py && cp /tmp/compliance_init.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/__init__.py`, then verify `wc -l` reads 18.
- **P1** | `src/lub/domains/__init__.py:3` | **NEW THIS CYCLE.** `SyntaxError: unterminated triple-quoted string literal (detected at line 10) at line 3`. On-disk 9 lines / 502 bytes; mtime `2026-04-26 13:51:49`. Canonical 17 lines / 502 bytes — same byte-equal/content-differs signature. | Temp-then-`cp` from `90e281b`; verify `wc -l` reads 17.
- **P1** | `src/lub/agents/__init__.py:1` | `SyntaxError: unterminated triple-quoted string literal (detected at line 41) at line 1`. 40 lines / 1,505 bytes; mtime `2026-04-27 05:08:03` (unchanged ~8 h). Canonical 56 lines / 1,505 bytes. | Temp-then-`cp` from `90e281b`; verify 56 lines.
- **P1** | `src/lub/agents/adapters/langgraph.py:77` | `SyntaxError: unterminated triple-quoted string literal (detected at line 82) at line 77`. 81 lines / 2,723 bytes; mtime `2026-04-27 09:10:35` (**17th consecutive cycle** flagged; in-place restore at 09:10 UTC failed). Canonical 82 lines / 2,723 bytes. | Temp-then-`cp` from `90e281b` strictly — plain `git show … > <repo path>` is empirically unsafe on this mount.
- **P1** | `src/lub/compliance/frameworks/bcb_4893.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. 18 lines / 550 bytes; mtime `2026-04-26 13:48:42` (unchanged). Canonical 20 lines / 550 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/bcbs_239.py:24` | `SyntaxError: unterminated triple-quoted string literal (detected at line 26) at line 24`. 25 lines / 797 bytes; mtime `2026-04-26 13:48:37` (unchanged). Canonical 27 lines / 797 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/eu_ai_act.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. 18 lines / 541 bytes; mtime `2026-04-26 13:48:31` (unchanged). Canonical 20 lines / 541 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/iso_23894.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. 18 lines / 545 bytes; mtime `2026-04-26 13:48:47` (unchanged). Canonical 20 lines / 545 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/sr_11_7.py:30` | `SyntaxError: unterminated triple-quoted string literal (detected at line 32) at line 30`. 31 lines / 1,119 bytes; mtime `2026-04-26 13:48:20` (unchanged). Canonical 32 lines / 1,119 bytes. | Temp-then-`cp` from `90e281b`.

`git log --since="24 hours ago" --oneline` (run from `C:\code\eb2niw`) returns the same four commits as recent cycles: `f91faf0 dfas`, `4c4805e Merge branch 'main' …`, `90e281b Atualização do workspace`, `f5e0bb3 Update workspace files`. `HEAD` is unchanged at `f91faf0`.

`git status --short` continues to fail: `fatal: unknown index entry format 0x70680000` (same value as the prior cycle; signature has been flapping between `0x70680000` and `0x3a860000` for several hours).

The two new flags are particularly interesting: their mtimes (`2026-04-26 13:51:49`) are **older** than several mtimes in last cycle’s flagged set, and they were **not** flagged in any of the 17 prior reports. Because `scripts/check_integrity.py` itself has been unchanged since `2026-04-26 13:53:29`, the most plausible explanation is silent in-place corruption that did **not** bump mtime — i.e. content drift after the OS metadata was last cached, the same late-stage symptom Rodrigo has flagged before. Worth an extra defensive `find … -newer` check during the next manual repair window.

The `llm-uncertainty-banking` subdirectory is **not** its own git repo (no nested `.git`); all `git show` / `git log` recovery has to be run from `C:\code\eb2niw`.

## 2. AST sweep + Python 3.11+ usage

Skipped — gated by step 1.

## 3. Architectural drift

Skipped — gated by step 1.

## 4. Code-smell + duplication

Skipped — gated by step 1.

## 5. Backend capability + estimator coverage

Skipped — gated by step 1.

## 6. Test coverage by subpackage

Skipped — gated by step 1.

## 7. Recent-activity recheck (last 24h)

Skipped — gated by step 1. Note: none of the nine corrupt files have new mtimes since the prior cycle, so recent-activity has nothing to recheck even if step 1 had passed.

---

## Top 5 next moves

1. **Bulk-restore all nine files in one `C:\code\eb2niw` shell session** using the temp-then-`cp` pattern: write each canonical blob from `90e281b` into `/tmp/`, then `cp` into the working tree; verify with `python scripts/check_integrity.py` exiting 0. This consolidates four prior Top-5 items now that the corruption set has grown to nine. (supersedes prior items 1–3)
2. **Investigate silent corruption with no mtime bump** — the two new flags (`compliance/__init__.py`, `domains/__init__.py`) were valid through the 12:16 cycle and are corrupt now, but their mtimes (`2026-04-26 13:51:49`) did not advance. Add a content-hash column to `scripts/check_integrity.py` so the next cycle can flag drift even when mtime lies. (NEW)
3. **Investigate the recurring `git status` index corruption** (`unknown index entry format 0x70680000` ↔ `0x3a860000`). Try `git update-index --refresh` from a Windows shell, or rebuild the index from a fresh clone, before the next scan window. (unchanged)
4. **After step 1 lands**, immediately run `python -c "import lub.agents, lub.compliance, lub.compliance.frameworks.bcb_4893, lub.domains"` to confirm imports resolve before the next hourly scan. (unchanged-spirit, expanded to cover new packages)
5. **Pin a watchdog**: schedule `find src/lub -newer scripts/check_integrity.py -name '*.py'` every cycle and persist the SHA-256 digest of every `*.py` under `src/lub/` to `reports/code_health/digests.json`; future scans compare against the last clean digest set. This is the only way to catch the no-mtime-bump corruption mode without depending on `90e281b` always being the latest clean reference. (NEW)

---

*Compared to previous report (`2026-04-27_1216.md`):*
- Same: 7 P1 findings (`agents/__init__.py`, `agents/adapters/langgraph.py`, the five `compliance/frameworks/*.py` files) — same paths, same on-disk byte-counts and line-counts, same mtimes, same canonical-blob recovery targets. Top 5 items 3 and 4 carry forward in spirit. `git status` index error format unchanged at `0x70680000`.
- Resolved: none.
- New: 2 P1 findings — `src/lub/compliance/__init__.py` and `src/lub/domains/__init__.py`, both with old (`2026-04-26 13:51:49`) mtimes that do **not** reflect the corruption event; both fit the byte-equal/content-differs partial-write signature; both recoverable from `90e281b`. Top 5 items 1, 2, 5 are new this cycle (item 1 consolidates three prior items into one bulk-restore now that the set is large enough that one-by-one repair is no longer the right unit of work).

---

*Executive summary:* 17th consecutive REPAIR NEEDED cycle and the corruption set just grew from 7 → 9 — two new flags in `lub.compliance.__init__` and `lub.domains.__init__` arrived with stale mtimes, suggesting silent content drift; bulk restore from `90e281b` still required and a content-hash watchdog is now warranted.
