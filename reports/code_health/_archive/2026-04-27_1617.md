# Code Health Scan — 2026-04-27 16:17 (UTC)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 11 P1 findings — corruption set **shrank by 1** since prior cycle (`2026-04-27_1516.md`); `tests/unit/test_extension_namespaces.py` (the lone NEW item from last cycle) was repaired and extended, while the 11 long-standing flagged files in `src/lub/{agents,compliance,domains}` remain untouched. Steps 2–7 skipped per the gating rule.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1** after scanning **342** `.py` files across 2 roots (file count unchanged; flagged count 11 vs. 12 prior). All eleven flagged paths still exhibit the canonical Windows + bash-mount partial-write signature: **byte-count on disk equals canonical blob byte-count, but on-disk line count is shorter** (truncation followed by NUL-padding to original size). This is now the **20th consecutive `REPAIR NEEDED` cycle**; the 11-file `src/` core is locked in — no mtime has moved on these files since cycle 17 (oldest stamps `2026-04-26 13:48:20`, newest `2026-04-27 09:10:35`).

`tests/unit/test_extension_namespaces.py` — flagged for the first time at the 15:16 cycle — now reads **122 lines / 5090 bytes** (mtime `2026-04-27 15:17:35`, ~2 minutes after the prior scan completed). Canonical at `90e281b` is 94 lines / 3756 bytes, so the on-disk file is now both line-count-larger and byte-count-larger than canonical, i.e. it was repaired *and* extended by ~28 lines of new content rather than merely restored. AST parses (verified by `check_integrity` no longer flagging it). This is the **first resolved corruption finding in 20 cycles** — partial recovery, but the `src/` set is unchanged.

Per-file detail (all 11 are byte-equal to canonical, line-count short of canonical — classic partial-write):

- **P1** | `src/lub/agents/__init__.py:1` | `SyntaxError: unterminated triple-quoted string literal (detected at line 41) at line 1`. 40 lines / 1,505 bytes; mtime `2026-04-27 05:08:03` (unchanged ~11 h, **20th** consecutive cycle flagged). Canonical 56 lines / 1,505 bytes. | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py > /tmp/agents_init.py && cp /tmp/agents_init.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py`; verify `wc -l` reads 56.
- **P1** | `src/lub/agents/adapters/langgraph.py:77` | `SyntaxError: unterminated triple-quoted string literal (detected at line 82) at line 77`. 81 lines / 2,723 bytes; mtime `2026-04-27 09:10:35` (20th consecutive cycle). Canonical 82 lines / 2,723 bytes. | Temp-then-`cp` from `90e281b`; plain `git show … > <repo path>` is empirically unsafe on this mount.
- **P1** | `src/lub/compliance/__init__.py:3` | `SyntaxError: unterminated triple-quoted string literal (detected at line 14) at line 3`. 13 lines / 621 bytes; mtime `2026-04-26 13:51:49` (4th consecutive cycle). Canonical 18 lines / 621 bytes. | Temp-then-`cp` from `90e281b`; verify `wc -l` reads 18.
- **P1** | `src/lub/compliance/frameworks/bcb_4893.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. 18 lines / 550 bytes; mtime `2026-04-26 13:48:42` (unchanged). Canonical 20 lines / 550 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/bcbs_239.py:24` | `SyntaxError: unterminated triple-quoted string literal (detected at line 26) at line 24`. 25 lines / 797 bytes; mtime `2026-04-26 13:48:37` (unchanged). Canonical 27 lines / 797 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/eu_ai_act.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. 18 lines / 541 bytes; mtime `2026-04-26 13:48:31` (unchanged). Canonical 20 lines / 541 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/iso_23894.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. 18 lines / 545 bytes; mtime `2026-04-26 13:48:47` (unchanged). Canonical 20 lines / 545 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/iso_42001.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17`. 18 lines / 549 bytes; mtime `2026-04-26 13:48:51` (2nd consecutive cycle). Canonical 20 lines / 549 bytes. | Temp-then-`cp` from `90e281b`; verify `wc -l` reads 20.
- **P1** | `src/lub/compliance/frameworks/nist_airmf.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 20) at line 17`. 19 lines / 645 bytes; mtime `2026-04-26 13:48:26` (2nd consecutive cycle). Canonical 21 lines / 645 bytes. | Temp-then-`cp` from `90e281b`; verify `wc -l` reads 21.
- **P1** | `src/lub/compliance/frameworks/sr_11_7.py:30` | `SyntaxError: unterminated triple-quoted string literal (detected at line 32) at line 30`. 31 lines / 1,119 bytes; mtime `2026-04-26 13:48:20` (unchanged). Canonical 32 lines / 1,119 bytes. | Temp-then-`cp` from `90e281b`.
- **P1** | `src/lub/domains/__init__.py:3` | `SyntaxError: unterminated triple-quoted string literal (detected at line 10) at line 3`. 9 lines / 502 bytes; mtime `2026-04-26 13:51:49` (4th consecutive cycle). Canonical 17 lines / 502 bytes. | Temp-then-`cp` from `90e281b`; verify `wc -l` reads 17.

`git log --since="24 hours ago" --oneline` (run from `C:\code\eb2niw`) returns the same four commits as recent cycles: `f91faf0 dfas`, `4c4805e Merge branch 'main' …`, `90e281b Atualização do workspace`, `f5e0bb3 Update workspace files`. `HEAD` unchanged at `f91faf028de`.

`git status --short` continues to fail: index error this cycle reads `fatal: unknown index entry format 0x70680000` — **same signature as the prior cycle**, suggesting the index file is stable for now (the multi-cycle flapping pattern `0x70680000` → `0x3a860000` → `0x02000000` → `0x70680000` → `0x70680000` may be settling). No path-level git operations are possible until the index is rebuilt (`git reset` from a clean parent shell, or delete `.git/index` and `git read-tree HEAD`).

The `llm-uncertainty-banking` subdirectory is **not** its own git repo; all `git show` / `git log` recovery has to be run from `C:\code\eb2niw`.

## 2. AST sweep + Python 3.11+ usage

Skipped — gated by step 1.

## 3. Architectural drift

Skipped — gated by step 1.

## 4. Code-smell + duplication

Skipped — gated by step 1.

## 5. Backend capability + estimator coverage

Skipped — gated by step 1.

## 6. Test coverage by subpackage

Skipped — gated by step 1.

## 7. Recent-activity recheck (last 24h)

Folded into §1: `tests/unit/test_extension_namespaces.py` was the only file modified inside the corruption-tracked set since the prior scan, and its repair is recorded above.

## Top 5 next moves

1. **Restore the 11 corrupted source files in one batch from `C:\code\eb2niw`** — a single bash session with `for f in <list>; do git show 90e281b:"$f" > /tmp/$(basename "$f"); cp /tmp/$(basename "$f") "$f"; done` followed by `python scripts/check_integrity.py` to verify exit 0. This is the same recommendation as the previous 19 cycles. **(unchanged)**
2. **Investigate the partial-write source process** — the `tests/unit/test_extension_namespaces.py` repair plus extension proves the file system *can* hold canonical content; corruption is recurring on a specific 11-file set whose mtimes haven't moved in ~24 h, suggesting the failed write happened once per file and the OS/AV/mount layer is now leaving them alone. Worth diffing `Get-Process` / Defender exclusions vs. last green cycle (cycle 0 — pre-2026-04-26 13:48). **(unchanged)**
3. **Repair the `.git/index`** — `fatal: unknown index entry format 0x70680000` is repeating, so a clean rebuild (`rm .git/index && git read-tree HEAD && git status`) from a non-bash-mount shell would unblock `git status` / `git diff` for future scans. **(unchanged)**
4. **Add a one-shot recovery script** at `scripts/restore_corrupted.sh` that takes the canonical commit (default `90e281b`) and restores the known-flapping file set via temp-then-`cp`. Currently this 11-line loop is reconstructed by hand each cycle. **(unchanged)**
5. **Reduce scan cadence to 4 h until corruption stabilises** — 20 hourly cycles produce 20 nearly-identical reports with zero state movement on the `src/` set; the signal-to-noise is poor. Switch to `0 */4 * * *` (or skip cycles where `git rev-parse HEAD` matches the prior report). **(unchanged)**

---

*Compared to previous report (`2026-04-27_1516.md`):*
- **Same:** 11 P1 findings on `src/lub/agents/{__init__.py,adapters/langgraph.py}`, `src/lub/compliance/{__init__.py,frameworks/{bcb_4893,bcbs_239,eu_ai_act,iso_23894,iso_42001,nist_airmf,sr_11_7}.py}`, `src/lub/domains/__init__.py`. All 11 mtimes unchanged; all Top 5 items unchanged.
- **Resolved:** `tests/unit/test_extension_namespaces.py` — repaired (94+ lines, AST-clean) and extended to 122 lines / 5090 bytes (vs. canonical 94 lines / 3756 bytes), mtime `2026-04-27 15:17:35`. Whoever fixed it added ~28 lines of new test content beyond restoration; check_integrity no longer flags it. **First resolved finding in 20 cycles.**
- **New:** none.
- **Index signature:** `0x70680000` repeats from prior cycle (no flap this hour).

20th consecutive 🔴 REPAIR NEEDED cycle: 11 corrupt source files (unchanged set, unchanged mtimes), but `tests/unit/test_extension_namespaces.py` was repaired and extended — first recovery in 20 cycles.
