# Code Health Scan — 2026-04-26 23:50 (UTC)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 2 P1 findings (disk-corruption / mid-statement truncation in two `.py` files). Steps 2–7 skipped per gating rule — see "Top 5 next moves" below.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1** after scanning 340 `.py` files across 2 roots. Two files failed AST/syntax parsing — both truncate mid-statement at end-of-file, the canonical fingerprint of the recurring Windows + bash-mount corruption. No repairs were attempted (read-only scan).

Flagged files (with parser-reported failure point and trailing bytes):

- **P1** | `src/lub/compliance/frameworks/__init__.py:41` | `SyntaxError: '(' was never closed at line 41` — file is 42 lines / 2,066 bytes and ends mid-`from … import (` after only `bcb_4893,` is listed; trailing bytes are `…import (  # noqa: F401\n    bcb_4893,\n ` with no closing paren and no further re-exports. | Restore the full multi-import block (one entry per pre-shipped framework module: `bcb_4893, bcbs_239, eu_ai_act, iso_23894, iso_42001, nist_airmf, sr_11_7`) plus the `ComplianceFrameworkProtocol` re-export from `protocols.py` and the `__all__` list (the test file at `tests/unit/test_compliance_frameworks.py:148–150` asserts `"ComplianceFrameworkProtocol" in frameworks.__all__`). Source of truth: the module's own docstring lists the seven framework modules; cross-check against `src/lub/compliance/frameworks/protocols.py` and the seven sibling files actually present on disk.
- **P1** | `tests/unit/test_compliance_frameworks.py:151` | `SyntaxError: invalid syntax at line 151` — file is 150 lines / 5,956 bytes and ends mid-`from` statement; trailing bytes are `…    assert "ComplianceFrameworkProtocol" in frameworks.__all__\n    # And the alias is the same object as the canonical home.\n    from lub.c` with the import line cut off after two characters. | Reconstruct the final test (`test_compliance_framework_protocol_is_re_exported`); the missing tail is approximately `from lub.compliance.frameworks.protocols import ComplianceFrameworkProtocol as Canonical\n    assert frameworks.ComplianceFrameworkProtocol is Canonical\n` (verify exact symbol names against `src/lub/compliance/frameworks/protocols.py`).

`git status --short` confirms **both files are untracked** (`?? src/lub/compliance/frameworks/__init__.py`, `?? tests/unit/test_compliance_frameworks.py`) — there is no committed copy to `git checkout`. Recovery must come from IDE local-history, a non-mounted backup, or hand-reconstruction from the docstring + sibling modules + the surviving test prefix (lines 1–150 still parse on their own; only the final test is incomplete).

For context on the broader working tree: `git status --short` against `src/` and `tests/` shows ~205 modified entries (none of them deletions of these two files, since the files were never committed in the first place). No commits in the last 24h: `git log --since="24 hours ago" --oneline` returns only `f5e0bb3 Update workspace files`.

## 2. AST sweep + Python 3.11+ usage

Skipped — gated by step 1 (per `CODE_HEALTH_SCAN_PROMPT.md`). Will run on next scan once integrity check exits 0.

## 3. Architectural drift

Skipped — gated by step 1.

## 4. Code-smell + duplication

Skipped — gated by step 1.

## 5. Backend capability + estimator coverage

Skipped — gated by step 1.

## 6. Test coverage by subpackage

Skipped — gated by step 1.

## 7. Recent-activity recheck (last 24h)

Skipped — gated by step 1. Only one commit landed in the last 24h (`f5e0bb3 Update workspace files`); even if steps 2–7 had run, the recheck set would have been minimal.

## Top 5 next moves

1. **Reconstruct `src/lub/compliance/frameworks/__init__.py`.** Untracked, so `git checkout` is a no-op. Sources of truth, in order of preference: (a) IDE local-history / `.idea/shelf` or VSCode timeline; (b) a fresh copy from a non-mounted path; (c) regenerate the import block + `__all__` from the docstring (lists the seven framework modules) and the `ComplianceFrameworkProtocol` reference in `lub.compliance.frameworks.protocols`. The seven sibling modules (`bcb_4893.py`, `bcbs_239.py`, `eu_ai_act.py`, `iso_23894.py`, `iso_42001.py`, `nist_airmf.py`, `sr_11_7.py`) are the authoritative target list. **(unchanged)**
2. **Reconstruct `tests/unit/test_compliance_frameworks.py`.** New finding this cycle. Lines 1–150 still parse and represent ≥95% of the file; only the trailing test (`test_compliance_framework_protocol_is_re_exported`) needs the last two lines completed. Easiest path: complete the dangling `from lub.c…` import + the identity assertion, then re-run `python -c "import ast; ast.parse(open('tests/unit/test_compliance_frameworks.py').read())"`.
3. Re-run `python scripts/check_integrity.py` and confirm exit 0 before the next scheduled scan; only then will steps 2–7 produce a real baseline. **(unchanged)**
4. After restoration, `git add` **both** files. The pattern of losing untracked package-init/test files to mount corruption keeps recurring (this report's two files, plus the `__init__.py` flagged in `2026-04-26_2338.md`). Add the `CONTRIBUTING.md` invariant proposed in earlier reports — "every `src/lub/**.py` and `tests/**.py` must be tracked before being trusted" — and consider a one-shot audit: `comm -23 <(find src/lub tests -name '*.py' | sort) <(git ls-files src/lub tests | grep '\.py$' | sort)` to find every untracked `.py` in scope. **(unchanged in spirit)**
5. Add a pre-commit hook (or extend `.pre-commit-config.yaml`) that runs `scripts/check_integrity.py` on staged `.py` files; this is the cheapest way to catch a mid-write truncation before it becomes the next morning's scan finding. **(unchanged)**

---

*Compared to previous report (`2026-04-26_2338.md`):*
- Same: gating step still failing; recovery still requires non-git sources because the flagged files are untracked. Top-5 items #1, #3, #4, #5 are carried over **(unchanged)**.
- Resolved: none — no code changes since the previous scan (one commit, `f5e0bb3 Update workspace files`, but it touched no `.py` under `src/lub` or `tests`).
- New: `tests/unit/test_compliance_frameworks.py` has now been corrupted in addition to the previously flagged `src/lub/compliance/frameworks/__init__.py`. Same fingerprint (mid-statement truncation), different file — the underlying mount issue is **still active and producing fresh corruption between scans** (~12 min apart). This is a meaningful escalation: corruption is now hitting the test that would catch the corruption.

### Tool issues

None. `check_integrity.py`, `git`, and the AST imports needed for step 1 ran cleanly. (Steps 2–7 not exercised.)

---

*Disk corruption recurred between hourly scans — two untracked files (`src/lub/compliance/frameworks/__init__.py`, `tests/unit/test_compliance_frameworks.py`) are truncated mid-statement; the prior cycle's truncation is still unrepaired and a second file has joined it, so step 1 gates the rest of the scan again.*
