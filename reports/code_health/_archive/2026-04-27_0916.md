# Code Health Scan — 2026-04-27 09:16 (UTC)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 4 P1 findings — `langgraph.py` (13th cycle, just rewritten and **re-corrupted** at 09:10 UTC) plus three newly-surfaced corruptions: `agents/__init__.py`, `compliance/frameworks/bcbs_239.py`, `compliance/frameworks/sr_11_7.py`. Steps 2–7 skipped per the gating rule.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1** after scanning **341** `.py` files across 2 roots. Four files are now flagged (up from one in the previous twelve cycles):

- **P1** | `src/lub/agents/adapters/langgraph.py:77` | `SyntaxError: unterminated triple-quoted string literal (detected at line 82) at line 77`. File is 81 lines / 2,723 bytes; **mtime jumped to `2026-04-27 09:10:35` (~6 min before this scan), having been frozen at `2026-04-26 02:17:54` for the previous twelve cycles**. Tail is still `    DEFER (v` with no closing `"""`; canonical at `90e281b` is 82 lines / 2,723 bytes ending `…install 'llm-uncertainty-banking[agents-beta]'.")`. Byte-count-equal, content-differs — the canonical Windows + bash-mount partial-write failure mode. **Someone (or some automation) attempted a write at 09:10 and the write was itself truncated.** | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py > 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py`, then **immediately** `python 06_Projeto_GitHub/llm-uncertainty-banking/scripts/check_integrity.py` to verify the write landed intact (last attempt did not).
- **P1** | `src/lub/agents/__init__.py:1` | `SyntaxError: unterminated triple-quoted string literal (detected at line 41) at line 1`. File is 40 lines / 1,505 bytes; mtime `2026-04-27 05:08:03` (~4 h ago — predates the 06:16, 07:16, and 08:16 scans, which did **not** flag it). Canonical at `90e281b` is 56 lines / 1,505 bytes ending `"ReportingAgent", "RunReport", ]`. Same byte-count-equal, content-differs signature. Not picked up by prior cycles, which is itself a finding (see Top-5 #4). | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py > 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py`, then re-run `check_integrity.py`.
- **P1** | `src/lub/compliance/frameworks/bcbs_239.py:24` | `SyntaxError: unterminated triple-quoted string literal (detected at line 26) at line 24`. File is 25 lines / 797 bytes; mtime `2026-04-26 13:48:37` (~19 h ago — has been corrupt for at least nineteen scan cycles without ever being flagged). Canonical at `90e281b` is 27 lines / 797 bytes ending `__all__ = ["CROSSWALK_KEY", "REGIME", "TITLE", "get_controls"]`. Same byte-count-equal, content-differs signature. | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/bcbs_239.py > 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/bcbs_239.py`.
- **P1** | `src/lub/compliance/frameworks/sr_11_7.py:30` | `SyntaxError: unterminated triple-quoted string literal (detected at line 32) at line 30`. File is 31 lines / 1,119 bytes; mtime `2026-04-26 13:48:20` (~19 h ago — same situation as `bcbs_239.py`). Canonical at `90e281b` is 32 lines / 1,119 bytes ending `__all__ = ["CROSSWALK_KEY", "PILLARS", "REGIME", "TITLE", "get_controls"]`. Same byte-count-equal, content-differs signature. | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/sr_11_7.py > 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/sr_11_7.py`.

`git log --since="24 hours ago" --oneline` (parent `eb2niw` repo) returns the same four commits as the previous thirteen cycles: `f91faf0 dfas`, `4c4805e Merge branch 'main' …`, `90e281b Atualização do workspace`, `f5e0bb3 Update workspace files`. No new commits in the last hour. `HEAD` is `f91faf0` (unchanged). All four canonical blobs are recoverable from `90e281b`.

`git status --short` still fails: `fatal: unknown index entry format 0x70680000` (signature unchanged for three consecutive cycles — index is corrupt-but-static). `git log` and `git show` against committed blobs continue to work, so the recovery path is intact.

The `llm-uncertainty-banking` subdirectory is **not** its own git repo (no nested `.git`); all `git show`/`git log` recovery has to be run from `C:\code\eb2niw`.

## 2. AST sweep + Python 3.11+ usage

Skipped — gated by step 1.

## 3. Architectural drift

Skipped — gated by step 1.

## 4. Code-smell + duplication

Skipped — gated by step 1.

## 5. Backend capability + estimator coverage

Skipped — gated by step 1.

## 6. Test coverage by subpackage

Skipped — gated by step 1.

## 7. Recent-activity recheck (last 24h)

Skipped — gated by step 1. For reference, exactly one `.py` file has an mtime newer than the `2026-04-27_0816.md` report: `src/lub/agents/adapters/langgraph.py` at `2026-04-27 09:10:35`. The write happened, the byte count matched the canonical, but the content is still corrupt — the **same** truncated tail as the previous twelve cycles. This is the strongest evidence yet that the corruption is happening **at write time**, not at rest.

## Top 5 next moves

1. **Restore all four corrupted files in one pass, then verify before doing anything else.** From `C:\code\eb2niw`: `for p in 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/bcbs_239.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/sr_11_7.py; do git show 90e281b:$p > $p; done && python 06_Projeto_GitHub/llm-uncertainty-banking/scripts/check_integrity.py`. The last `langgraph.py` restore was itself truncated on write at 09:10 UTC — verify each file with `wc -l` against the canonical line counts (40 → 56, 81 → 82, 25 → 27, 31 → 32) **before** trusting the result.
2. **Treat the 09:10 UTC re-corruption of `langgraph.py` as a write-time failure, not a one-off.** The file was overwritten at 09:10 with bytes whose count matches the canonical but whose tail is still truncated — same signature as the original corruption. This means a bare `git show … > …` on the bash mount is not a safe recovery primitive on this host. Mitigation: write to a temp path on a non-mount filesystem first (`/tmp` works inside the workspace VM) and `cp` into the repo, or use `git restore --source=90e281b -- <path>` from the repo root rather than shell redirection.
3. **Repair the parent `eb2niw` `.git/index` (unchanged — tenth consecutive cycle).** Signature stable at `0x70680000` for three cycles. Recovery: from `C:\code\eb2niw`, `rm .git/index && git reset`. Until done, no scan can produce a `git status --short` snapshot, so newly-modified-but-uncommitted files are invisible to the recheck pass.
4. **Investigate why three corruptions sat undetected for ≥4 hours and ≥19 hours respectively (unchanged in spirit — Top-5 #4 has flagged `check_integrity.py` for four cycles).** `agents/__init__.py` (mtime 05:08) and the two compliance files (mtime 13:48 yesterday) were corrupt across multiple prior scans yet never flagged. The script itself reports "scanned 341 .py files in 2 root(s)" consistently, so the scope looks unchanged — the most likely explanations are (a) the script was short-circuiting after the first failure in earlier cycles, (b) a caching/symlink artifact on the bash mount, or (c) the prior corruptions were transient and only stabilized this cycle. Diff `scripts/check_integrity.py` between today and the version active during the past twelve cycles to confirm.
5. **Wire `check_integrity.py` into a pre-write hook (unchanged — twelfth consecutive cycle).** Strengthened by the 09:10 langgraph.py event: writes to repo paths on this host are demonstrably unreliable, so a post-write-verify hook would have caught the bad restore immediately rather than letting it sit until the next hourly scan.

---

*Compared to previous report (`2026-04-27_0816.md`):*
- Same: HEAD still `f91faf0`; same four commits in the 24h window; `git status` still rejects with `0x70680000`; `langgraph.py` still flagged with the same `SyntaxError` text and same byte size 2,723 (now on the 13th consecutive cycle). Top-5 items #3, #4, #5 carry over **(unchanged in substance)**.
- Resolved: nothing.
- New: **Three additional P1 findings** — `agents/__init__.py:1` (mtime 05:08, missed by 3 prior scans), `compliance/frameworks/bcbs_239.py:24` (mtime yesterday 13:48, missed by ≥19 scans), `compliance/frameworks/sr_11_7.py:30` (mtime yesterday 13:48, missed by ≥19 scans). `langgraph.py` mtime advanced to `2026-04-27 09:10:35` — first write to that file in 31 hours, but **the write itself produced corrupt bytes** (same truncated tail). Top-5 #2 is new and is the most actionable item this cycle.

### Tool issues

- `git status --short` and `git ls-files` against the parent repo still fail (`fatal: unknown index entry format 0x70680000`). `git log` and `git show` continue to work. Folded into Top-5 item #3.
- The `llm-uncertainty-banking` subdirectory is not its own git repo; all git operations must run from `C:\code\eb2niw`.

---

*Three new disk-corruption findings surfaced this cycle and the 09:10 UTC restore of `langgraph.py` produced corrupt bytes again — the bash-mount write path itself is the failure point, not file-at-rest decay.*
