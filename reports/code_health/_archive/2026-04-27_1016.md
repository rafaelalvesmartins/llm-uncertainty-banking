# Code Health Scan — 2026-04-27 10:16 (UTC)

**Status:** 🔴 REPAIR NEEDED

**Summary:** 7 P1 findings — the four corruptions from the previous cycle are all still present (none recovered) and three additional `compliance/frameworks/*.py` files are now flagged for the first time (`bcb_4893.py`, `eu_ai_act.py`, `iso_23894.py`). Steps 2–7 skipped per the gating rule.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited with status **1** after scanning **341** `.py` files across 2 roots. Seven files are flagged this cycle (up from four):

- **P1** | `src/lub/agents/__init__.py:1` | `SyntaxError: unterminated triple-quoted string literal (detected at line 41) at line 1`. File is 40 lines / 1,505 bytes; mtime `2026-04-27 05:08:03` (unchanged from previous cycle, ~5 h ago). Canonical at `90e281b` is 56 lines / 1,505 bytes. Byte-count-equal, content-differs — the canonical Windows + bash-mount partial-write failure mode. Restore step from previous report not yet executed. | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py > 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py`, then re-verify with `wc -l` (must read 56) and `python scripts/check_integrity.py`.
- **P1** | `src/lub/agents/adapters/langgraph.py:77` | `SyntaxError: unterminated triple-quoted string literal (detected at line 82) at line 77`. File is 81 lines / 2,723 bytes; mtime `2026-04-27 09:10:35` (unchanged from previous cycle — no new write attempts in the last hour). Canonical at `90e281b` is 82 lines / 2,723 bytes. **14th consecutive cycle flagged**, including the failed in-place restore at 09:10 UTC. | From `C:\code\eb2niw`: write to `/tmp/langgraph.py` first via `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py > /tmp/langgraph.py`, then `cp /tmp/langgraph.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py`, then verify line count (must read 82). Plain `git show … > <repo path>` is empirically unsafe on this mount.
- **P1** | `src/lub/compliance/frameworks/bcb_4893.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17` — **NEW this cycle**. File is 18 lines / 550 bytes; mtime `2026-04-26 13:48:42` (~21 h ago — has been corrupt for ≥21 prior scans without ever being flagged). Canonical at `90e281b` is 20 lines / 550 bytes ending `__all__ = ["CROSSWALK_KEY", "REGIME", "TITLE", "get_controls"]`. Same byte-count-equal, content-differs signature. | From `C:\code\eb2niw`: `git show 90e281b:06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/bcb_4893.py > /tmp/bcb_4893.py && cp /tmp/bcb_4893.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/bcb_4893.py`.
- **P1** | `src/lub/compliance/frameworks/bcbs_239.py:24` | `SyntaxError: unterminated triple-quoted string literal (detected at line 26) at line 24`. File is 25 lines / 797 bytes; mtime `2026-04-26 13:48:37` (unchanged from previous cycle). Canonical 27 lines / 797 bytes. | Same temp-then-`cp` recovery as above; canonical commit `90e281b`.
- **P1** | `src/lub/compliance/frameworks/eu_ai_act.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17` — **NEW this cycle**. File is 18 lines / 541 bytes; mtime `2026-04-26 13:48:31` (~21 h ago — same long-tail miss as `bcb_4893.py`). Canonical 20 lines / 541 bytes. | Same temp-then-`cp` recovery from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/iso_23894.py:17` | `SyntaxError: unterminated triple-quoted string literal (detected at line 19) at line 17` — **NEW this cycle**. File is 18 lines / 545 bytes; mtime `2026-04-26 13:48:47` (~21 h ago). Canonical 20 lines / 545 bytes. | Same temp-then-`cp` recovery from `90e281b`.
- **P1** | `src/lub/compliance/frameworks/sr_11_7.py:30` | `SyntaxError: unterminated triple-quoted string literal (detected at line 32) at line 30`. File is 31 lines / 1,119 bytes; mtime `2026-04-26 13:48:20` (unchanged from previous cycle). Canonical 32 lines / 1,119 bytes. | Same temp-then-`cp` recovery from `90e281b`.

`git log --since="24 hours ago" --oneline` returns the same four commits as the previous cycles: `f91faf0 dfas`, `4c4805e Merge branch 'main' …`, `90e281b Atualização do workspace`, `f5e0bb3 Update workspace files`. No new commits in the last hour. `HEAD` is `f91faf0` (unchanged). All seven canonical blobs are recoverable from `90e281b` (size match confirmed).

`git status --short` still fails: `fatal: unknown index entry format 0x70680000` (signature unchanged for four consecutive cycles — index is corrupt-but-static). `git log` and `git show` against committed blobs continue to work, so the recovery path is intact.

The `llm-uncertainty-banking` subdirectory is **not** its own git repo (no nested `.git`); all `git show`/`git log` recovery has to be run from `C:\code\eb2niw`.

## 2. AST sweep + Python 3.11+ usage

Skipped — gated by step 1.

## 3. Architectural drift

Skipped — gated by step 1.

## 4. Code-smell + duplication

Skipped — gated by step 1.

## 5. Backend capability + estimator coverage

Skipped — gated by step 1.

## 6. Test coverage by subpackage

Skipped — gated by step 1.

## 7. Recent-activity recheck (last 24h)

Skipped — gated by step 1. For reference, no `.py` file under `src/lub/` has an mtime newer than the `2026-04-27_0916.md` report — nothing has been written to the tree in the last hour, so neither the four pre-existing corruptions nor the three newly-flagged ones can be attributed to a write event during this cycle. The three new compliance findings have mtimes from `2026-04-26 13:48` (~21 h ago) and were corrupt the entire time; they were simply not surfaced by `check_integrity.py` until now. This is a direct continuation of Top-5 #4 from the previous cycle.

## Top 5 next moves

1. **Restore all seven corrupted files in one pass via the `/tmp` indirection, then verify line counts before trusting the result.** From `C:\code\eb2niw`: `for p in 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/__init__.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/bcb_4893.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/bcbs_239.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/eu_ai_act.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/iso_23894.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/sr_11_7.py; do git show 90e281b:$p > /tmp/r.py && cp /tmp/r.py $p; done && python 06_Projeto_GitHub/llm-uncertainty-banking/scripts/check_integrity.py`. Expected post-restore line counts: 56 / 82 / 20 / 27 / 20 / 20 / 32. Use `wc -l` to confirm each before declaring success.
2. **Treat the bash-mount write path as unreliable; use `/tmp` staging for every restore (unchanged).** The 09:10 UTC `langgraph.py` failure remains the proof point — bare `git show … > <repo path>` produced byte-count-equal-but-content-corrupt output. `git restore --source=90e281b -- <path>` from the repo root is the safer git-native alternative, but the parent `eb2niw` index is broken (Top-5 #3) so `git restore` will refuse to run until that's fixed. Until then, `/tmp` indirection is the only reliable primitive.
3. **Repair the parent `eb2niw` `.git/index` (unchanged — eleventh consecutive cycle).** Signature stable at `0x70680000` for four cycles. Recovery: from `C:\code\eb2niw`, `rm .git/index && git reset`. Until done, `git restore` won't work and `git status --short` snapshots can't be taken.
4. **Investigate why `check_integrity.py` is surfacing pre-existing corruptions in batches rather than at write time (unchanged).** Three files corrupt since `2026-04-26 13:48` were missed by ~21 prior scans then flagged together this cycle, exactly mirroring the previous cycle's batch surfacing of three other files. The script consistently reports "scanned 341 .py files in 2 root(s)" so scope is stable; the most likely explanations remain (a) early-exit short-circuit on first failure, (b) bash-mount caching, or (c) staggered stabilization of partial writes. Diff `scripts/check_integrity.py` against last week's version and add per-file logging if the current behavior is not by design.
5. **Wire `check_integrity.py` into a pre-commit + post-write hook (unchanged — thirteenth consecutive cycle).** The 09:10 UTC `langgraph.py` re-corruption (untouched this cycle, so not retried) and the 21-hour-old compliance-frameworks corruptions both argue for the same remedy: verify-immediately-after-write, fail loud, refuse to proceed.

---

*Compared to previous report (`2026-04-27_0916.md`):*
- Same: `HEAD` still `f91faf0`; same four commits in the 24h window; `git status` still rejects with `0x70680000`; the four files flagged at 09:16 UTC are still flagged with identical byte sizes, mtimes, and `SyntaxError` text — no recovery executed in the last hour. Top-5 items #2, #3, #4, #5 carry over **(unchanged in substance)**.
- Resolved: nothing.
- New: **Three additional P1 findings** — `compliance/frameworks/bcb_4893.py:17` (mtime yesterday 13:48, missed by ≥21 prior scans), `compliance/frameworks/eu_ai_act.py:17` (mtime yesterday 13:48, missed by ≥21 prior scans), `compliance/frameworks/iso_23894.py:17` (mtime yesterday 13:48, missed by ≥21 prior scans). All three share the same canonical-tail (`__all__ = ["CROSSWALK_KEY", "REGIME", "TITLE", "get_controls"]`) and the same byte-count-equal, content-differs signature as the four already-known files. Top-5 #1 expanded from four files to seven.

### Tool issues

- `git status --short` and `git ls-files` against the parent repo still fail (`fatal: unknown index entry format 0x70680000`). `git log` and `git show` continue to work. Folded into Top-5 item #3.
- The `llm-uncertainty-banking` subdirectory is not its own git repo; all git operations must run from `C:\code\eb2niw`.

---

*Three more `compliance/frameworks/*.py` files surfaced as corrupt this cycle (now seven total flagged, all with byte-count-equal/content-differs signature, all recoverable from `90e281b`); none of last cycle's four were repaired in the intervening hour — the bash-mount write path remains the failure point.*
