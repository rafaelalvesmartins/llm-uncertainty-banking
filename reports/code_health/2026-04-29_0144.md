# Code Health Scan — 2026-04-29 01:44 local

**Status:** 🟠 RECOMMENDED

**Summary:** 0 P1, 1 P2, 5 P3 findings — see "Top 5 next moves".

---

## 1. Disk integrity

`scripts/check_integrity.py` reports FAIL with three "unterminated string
literal" SyntaxErrors:

- `src/lub/agents/adapters/crewai.py:60`
- `src/lub/agents/adapters/langgraph.py:93`
- `src/lub/compliance/frameworks/sr_11_7.py:42`

**Cross-checked via the Read tool: all three files are intact and
parseable end-to-end.** crewai.py ends with a properly-closed string
+ paren on line 60; langgraph.py has a fully-formed `NotImplementedError`
on lines 92-94; sr_11_7.py has a complete `__all__` on line 42. Per
the task's bash-mount truncation heuristic, these are sync-lag
artifacts of the bash sandbox reading through the Windows mount, **not
on-disk corruption** — see "Tool issues" below. No findings here.

This is the same false positive that has produced 9 consecutive
identical "REPAIR NEEDED" reports (1616 → 0120). The idempotence
guard does NOT fire because the *correct* finding-set differs from
the prior reports' Top 5: the prior reports treated the sync-lag as
real corruption.

## 2. AST sweep + Python 3.11+ usage

343 `.py` files in `src/lub/` + `tests/`; AST sweep clean once
sync-lag artifacts are excluded.

Python 3.11+ usage that blocks a 3.10 sandbox:

- **P3** | `src/lub/cli/benchmark.py:8` | `import tomllib` (stdlib only on 3.11+) | If 3.10 must be supported, switch to `tomli` for 3.10 + `tomllib` for 3.11 (already done in `dashboard.py:221` via try/except). Same pattern needed in 4 more files (see Details).

No `datetime.UTC` or `typing.Self` usage detected.

## 3. Architectural drift

`docs/architecture.md` is present. Five subpackages under `src/lub/`
are NOT mentioned in it:

- **P3** | `docs/architecture.md` | Subpackages `challenge`, `cnh`, `governance`, `integrations`, `mcp` are not described in the layered-architecture diagram | Add a one-paragraph section per subpackage; map each to its layer (L1–L5) so import-linter contracts stay aligned with the doc.

Four import-linter contracts in `pyproject.toml` — present and intact:

1. Layered architecture: wrappers < uncertainty < calibration < benchmarks < reports
2. Trust layer (rails/policies/guard) must not leak into core
3. Orchestration layer must not be imported by core
4. Governance layer (calibration_targets) must not leak into core

Recent commits (last 24h, scope `src/lub/`): `b1fbe37 klk`, `f91faf0
dfas`, `90e281b Atualização do workspace`, `b97b746 do it all`.
Commit messages are uninformative; cannot confidently audit intent
from the log alone.

`git status --short` shows ~30+ uncommitted modifications across the
parent `eb2niw` repo (mostly `00_Meta/` docs, `.claude/` configs, and
unrelated `02_Evidencias_Profissionais/` files); only a handful are in
`06_Projeto_GitHub/llm-uncertainty-banking/`. Snapshot in Details.

## 4. Code-smell + duplication

- **P3** | `src/lub/uncertainty/semantic_entropy.py:91` | Inline `a.strip().lower() == b.strip().lower()` outside `_text_utils.py` / `correctness.py` (the canonical home for normalisation) | Replace with `lub._text_utils.normalize(a) == lub._text_utils.normalize(b)` or import the existing helper.
- **P3** | `src/lub/dashboard/render.py:88` | `<!-- TODO(supply-chain): pin chart.js@4.4.1 with SRI hash` is the only live TODO/FIXME/XXX in `src/lub/` | Generate the SRI hash and pin per the comment's existing instructions.
- **P3** | `src/lub/cli/__init__.py`, `src/lub/mcp/tools/challenge.py`, `src/lub/mcp/tools/context_autopilot.py`, `src/lub/orchestration/hooks.py`, `src/lub/reports/assessment.py` | `__all__` omits public top-level symbols (e.g. `EXIT_OK`/`EXIT_USER`/`EXIT_INTERNAL`/`configure_logging` in `cli/__init__.py`; 8 schema classes in `mcp/tools/challenge.py`) | Add the missing names to `__all__`, or prefix the symbols with `_` if they are truly private.

No findings for: inline entropy outside `_math_utils`, `except Exception: pass`, bare `except BaseException`, SQL via f-string / %-format / `.format()` / `+` concatenation. The 7 cross-class `._conn` accesses in `lub/challenge/**` are all explicitly `# noqa: SLF001 -- first-party extension`-tagged and not regressions.

Function-length scan: 5 functions exceed 100 lines (`benchmarks/runner.py::run` 125, `reports/assessment.py::build_assessment_results` 122, `challenge/context_autopilot/recall.py::detect_recall_risk` 113, `challenge/context_autopilot/ejection.py::eject_top_k` 103, `guard.py::_handle_reask` 101). All are within 1.25× the threshold; flagging only as a watch-list, not a finding.

## 5. Backend capability + estimator coverage

Backend `CAPABILITIES` declarations:

- `anthropic`: GENERATE
- `dummy`: GENERATE | LOGPROBS | EMBED
- `hf`: GENERATE | LOGPROBS | EMBED
- `openai`: GENERATE | EMBED *(no LOGPROBS)*
- `vllm`: GENERATE | LOGPROBS

Estimators that DO declare `REQUIRES_CAPABILITIES` (6 of 22):
`adaptive_conformal`, `conformal`, `conformal_sampling`,
`mondrian_conformal`, `graph_laplacian`, `mahalanobis`.

- **P2** | `src/lub/uncertainty/{ccp,claim_level,eigenscore,ensemble,epistemic_aleatoric,lmpolygraph,monte_carlo_dropout,p_true,perplexity,sar,self_certainty,self_consistency,semantic_entropy,sentence_sar,token_logprob,verbalized}.py` | 16 of 22 estimators are missing `REQUIRES_CAPABILITIES` entirely; estimators that clearly need LOGPROBS (`perplexity`, `token_logprob`, `p_true`, `semantic_entropy`, `sar`, `sentence_sar`) will silently accept incompatible backends like `openai` and `anthropic`, producing late-stage `AttributeError` instead of an upfront capability assertion | Add the appropriate `REQUIRES_CAPABILITIES` class attribute on each (start with the 6 LOGPROBS-dependent ones; the remaining 10 may be GENERATE-only and can be declared explicitly to confirm).

No declared (estimator, backend) pair is incompatible by construction — the gap is the *missing* declarations, not contradictions.

## 6. Test coverage by subpackage

Naive 1:1 `tests/unit/test_<module>.py` matching is misleading because
the suite uses thematic test names (e.g. `test_challenge_replay.py`
covers `challenge/replay.py`, `test_wrapper_backends.py` covers all
five backends). After cross-referencing actual test files:

- `wrappers/`: 7 src vs. 5 dedicated tests (api_base, api_base_retry, dummy_backend, hf_local, wrapper_backends) — adequate.
- `challenge/`: 4 top-level src + sub-modules vs. 7 challenge_*/context_autopilot_* tests — adequate.
- `mcp/`: 2 src vs. 3 tests (mcp_airmf, mcp_plugin_loader, mcp_ruflo_compat) — adequate.
- `cnh/`: 3 src files, 0 dedicated tests — gap.
- `integrations/`: 2 src files, 0 dedicated tests — gap.
- `cli/`: 6 src files vs. 3 cli_* tests — borderline.

- **P3** | `src/lub/cnh/`, `src/lub/integrations/` | No `tests/unit/` files reference these subpackages by name | Add at least one smoke test per subpackage to exercise the public surface.

## 7. Recent-activity recheck (last 24h)

20 `.py` files in `src/lub/` were modified in the last 24h (full list
in Details). Re-running AST parse via the Read tool on each shows
all parseable; bash-mount sync lag continues to flag the same 3 files
as truncated, again resolved by the heuristic. No new SQL-injection
or capability-mismatch patterns appeared. No new findings beyond
those already listed in §4–§5.

## Top 5 next moves

1. **Declare `REQUIRES_CAPABILITIES` on the 6 LOGPROBS-dependent estimators** (`perplexity`, `token_logprob`, `p_true`, `semantic_entropy`, `sar`, `sentence_sar`) so an `openai` or `anthropic` backend fails fast at pipeline construction instead of with a late `AttributeError`. Highest ROI in this report.
2. **Replace the inline `.strip().lower()` at `src/lub/uncertainty/semantic_entropy.py:91`** with the existing `lub._text_utils.normalize` helper. One-line change, kills the last duplication site.
3. **Document the 5 missing subpackages** (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) in `docs/architecture.md` so the layered-architecture story matches the actual tree.
4. **Backfill `__all__`** in `cli/__init__.py`, `mcp/tools/challenge.py`, `mcp/tools/context_autopilot.py`, `orchestration/hooks.py`, `reports/assessment.py` (or underscore-prefix the symbols that aren't meant to be public). Quiet, but it shapes the public API.
5. **Pin `chart.js@4.4.1` with SRI in `dashboard/render.py:88`** per the existing supply-chain TODO. Small but security-relevant.

---

*Compared to previous report (`2026-04-29_0120.md`):*
- Same: status remains 🟠; bash-mount sync lag still observed on the same three files (now correctly classified as a tool issue, not a finding).
- Resolved: removed the 5 phantom "restore truncated file" Top 5 items — those files are intact on disk.
- New: real findings surfaced (capability declarations gap, `__all__` gaps, semantic_entropy normalisation, architecture-doc drift, dashboard SRI TODO).

---

## Tool issues

- **bash-mount sync lag (Windows + WSL)**: `cat`, `python ast.parse`, and `scripts/check_integrity.py` invoked via the bash sandbox return truncated bytes for `crewai.py`, `langgraph.py`, and `sr_11_7.py`. The Read tool sees all three files intact end-to-end. Per the task's heuristic, these are NOT code findings. The sync lag is reproducible across consecutive scans and explains the prior report loop.

## Details

**Python 3.11+ `tomllib` users (file:line):**
`src/lub/cli/benchmark.py:8`, `src/lub/reports/crosswalk.py:33`,
`src/lub/reports/dashboard.py:221` (already guarded), `src/lub/reports/dashboard_sources.py:59` (already guarded), `src/lub/reports/mapping.py:26`.

**Files modified in last 24h (src/lub):** `agents/adapters/{autogen,crewai,langgraph}.py`, `agents/{core,policies,reporter}.py`, `benchmarks/__init__.py`, `cli/{answer,benchmark,inspect,report,run_swarm}.py`, `ledger/__init__.py`, `mcp/schemas.py`, `mcp/tools/{estimators,ruflo_compat}.py`, `orchestration/protocols.py`, `orchestration/__init__.py`, `reports/__init__.py`, `uncertainty/p_true.py`.

**`git status --short` snapshot (truncated to 30 lines):** ~30+ modified files in parent `eb2niw` repo; bulk are `00_Meta/` documentation, `.claude/` configs, and `02_Evidencias_Profissionais/` static assets — none in `src/lub/`. No staged commits. No commit attempted.

---

*Slack one-liner:* lub scan 🟠 — 1 P2 (16 estimators missing `REQUIRES_CAPABILITIES`) + 5 P3 polish items; the recurring "truncation" finding was a bash-mount sync-lag artifact (files intact), so the 9-cycle loop is broken.
