# Code Health Scan -- 2026-05-01 04:53 local

**Status:** 🟠 RECOMMENDED

**Summary:** 1 P1, 3 P2, 3 P3 findings — see "Top 5 next moves".

---

## 1. Disk integrity

`scripts/check_integrity.py` exits 0 (gating step passes). The script reports one FAIL on `src/lub/agents/adapters/langgraph.py` ("unterminated string literal at line 93"). Cross-checked via the Read tool: the file is intact end-to-end (95 lines; the multi-line `NotImplementedError` argument terminates cleanly on line 94 and EOF at line 95). This is the documented Windows-mount sync-lag artifact, NOT a code finding (see "Tool issues"). No real integrity failures.

## 2. AST sweep + Python 3.11+ usage

No genuine AST parse failures (only the langgraph.py sync-lag noise above). `tomllib` remains imported in `cli/benchmark.py`, `reports/crosswalk.py`, `reports/mapping.py`; `pyproject.toml` declares `requires-python>=3.11`, so this is intentional. No findings.

## 3. Architectural drift

Five subpackages remain absent from `docs/architecture.md`: **`challenge`**, **`cnh`**, **`governance`**, **`integrations`**, **`mcp`**. The only mention of "MCP" in `docs/architecture.md` (lines 271–272) refers to ruflo's auto-generated MCP surface, not `lub.mcp`. The four import-linter contracts in `pyproject.toml` are present and unchanged (lines 146, 164, 182, 197: layered-architecture, trust-layer, orchestration-layer, governance-layer). `git log --since="24 hours ago" --oneline` returned no commits. `git status --short` shows ~300 modified paths in the parent worktree (`00_Meta/`, `02_Evidencias_Profissionais/`, `.claude/`, plus several `src/lub/**/__init__.py` and CLI files); none introduce new top-level subpackages.

- **P2** | `docs/architecture.md` | five subpackages (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) are not described in the layered overview | add a short paragraph per subpackage placing each in L1–L5 (or trust/orchestration crosscut). **(unchanged)**
- **P3** | `docs/architecture.md:190` | the doc still describes `lub.domains` as "empty namespace package", but `src/lub/domains/banking/__init__.py` (added in pass 33, surfaced this run) ships a non-empty skeleton that lazy-aliases six benchmark modules (`finqa`, `convfinqa`, `tatqa`, `credit_scoring`, `financial_sentiment`, `br_regulatory`) | tighten the line to "namespace package; today ships `lub.domains.banking` as a lazy-alias skeleton over `lub.benchmarks.*`" or similar. **(new)**

## 4. Code-smell + duplication

- **P2** | `src/lub/uncertainty/semantic_entropy.py:91` | `a.strip().lower() == b.strip().lower()` duplicates the canonical normalizer outside `_text_utils.py` | replace with `normalize(a) == normalize(b)` from `lub._text_utils`. **(unchanged)**

Duplication searches clean: no inline `entropy = -sum(p * math.log(p)` outside `_math_utils.py`; no `except Exception: pass`; no bare `except BaseException`; no f-string / %-format / `.format()` SQL injection patterns; the three direct `_conn` accesses in `challenge/context_autopilot/{ejection,monitor}.py` and `challenge/meta_calibration.py` are all annotated `# noqa: SLF001` (first-party extension, acceptable). Six `TODO`/`FIXME`/`XXX` comments across five files; none rise to a finding on their own except the chart.js item below.

- **P3** | `src/lub/dashboard/render.py:92` | chart.js loaded from CDN with `crossorigin` but no SRI hash; in-file TODO at lines 88–91 documents the recipe | run the recipe and add `integrity="sha384-…"` to the `<script>` tag. **(unchanged)**

## 5. Backend capability + estimator coverage

Backends (`src/lub/wrappers/*.py`) declare:
- `dummy`, `hf`: GENERATE | LOGPROBS | EMBED
- `vllm`: GENERATE | LOGPROBS
- `openai`: GENERATE | EMBED (no LOGPROBS)
- `anthropic`: GENERATE only

Estimators with explicit `REQUIRES_CAPABILITIES`: `adaptive_conformal`, `conformal`, `conformal_sampling` (LOGPROBS); `graph_laplacian`, `mahalanobis` (EMBED); `mondrian_conformal` (LOGPROBS). Same set as prior reports.

- **P1** | `src/lub/uncertainty/{perplexity,token_logprob,p_true,semantic_entropy,sar,sentence_sar}.py` | six estimators that consume token-level logprobs do not declare `REQUIRES_CAPABILITIES`, so they inherit the base default (GENERATE | LOGPROBS via `wrappers/base.py`) and silently pair with `openai`/`anthropic` instead of failing fast at pipeline construction | add explicit `REQUIRES_CAPABILITIES = BackendCapability.GENERATE | BackendCapability.LOGPROBS` to each class so the requirement is locally visible and a future relaxation of the base default doesn't break correctness. **(unchanged)**

## 6. Test coverage by subpackage

By "tests referencing `lub.<sub>` anywhere under `tests/`" (broad pattern, consistent with prior reports):

| Subpackage | src files | referencing tests | ratio | status |
|---|---:|---:|---:|---|
| cnh | 3 | 1 | 33% | under |
| compliance | 8 | 3 | 37% | under |
| integrations | 2 | 1 | 50% | at threshold |
| dashboard | 7 | 6 | 85% | resolved |
| mcp | 8 | 8 | 100% | resolved |

`domains` has no non-init source files yet (the `banking/__init__.py` skeleton is pure lazy aliasing); 3 tests already touch it. Not flag-able under the current rule.

Movement since `2026-04-30_0642.md`:
- `compliance` rose from 2/8 (25%) to 3/8 (37%) — `tests/unit/test_compliance_frameworks.py` (added 2026-05-01 04:16) joins `test_extension_namespaces.py` and `tests/test_generic_namespaces.py`. Still under threshold.
- `dashboard` ticked up 5/7 → 6/7 (71% → 85%).
- `cnh` and `integrations` unchanged.

- **P2** | `tests/unit/{cnh,compliance}` | two subpackages remain below the 50% referencing-tests threshold (`cnh` 1/3, `compliance` 3/8) | add at minimum a smoke-import test (`from lub.<sub> import …`) per remaining source module; `compliance` is the larger gap (5 modules still uncovered). **(changed — `compliance` improved from 25% to 37% but still under)**

## 7. Recent-activity recheck (last 24h)

No commits in last 24h. Files modified in `src/lub/` and `tests/` since the prior report (mtime newer than `reports/code_health/2026-04-30_0642.md`):

- `src/lub/domains/banking/__init__.py` (new skeleton, 27 lines, lazy re-exports from `lub.benchmarks`)
- `tests/unit/test_compliance_frameworks.py` (new test, 7.7 KB, pins the lazy-alias contract for `lub.compliance.frameworks` and `lub.domains.banking`)

Both parse cleanly under `ast.parse` (verified directly with the Read tool given the documented bash-mount sync lag) and `check_integrity.py`. No new code-smell, capability-mismatch, or SQL-injection patterns. Per task constraints, no commit performed.

## Top 5 next moves

1. **Declare `REQUIRES_CAPABILITIES` on the 6 LOGPROBS-dependent estimators** (`perplexity`, `token_logprob`, `p_true`, `semantic_entropy`, `sar`, `sentence_sar`) so `openai`/`anthropic` backends fail fast at pipeline construction. **(unchanged)**
2. **Replace `a.strip().lower() == b.strip().lower()` at `src/lub/uncertainty/semantic_entropy.py:91`** with `normalize(a) == normalize(b)` from `lub._text_utils`. One-line change. **(unchanged)**
3. **Document the 5 missing subpackages** (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) in `docs/architecture.md`. **(unchanged)**
4. **Add smoke-import tests for `compliance` (3/8) and `cnh` (1/3)** — `compliance` now has one new test but 5 modules remain uncovered; `cnh` still at 1/3. **(changed — `compliance` improved 25% → 37%, still under)**
5. **Pin `chart.js@4.4.1` with SRI in `dashboard/render.py:92`** per the in-file recipe at lines 88–91. **(unchanged)**

Honourable mention (would be #6): tighten the `lub.domains` paragraph in `docs/architecture.md:190` so it reflects the new `lub.domains.banking` skeleton instead of saying "empty namespace package". 30-second edit.

---

*Compared to previous report (`2026-04-30_0642.md`):*
- Same: items 1, 2, 3, 5 (REQUIRES_CAPABILITIES on 6 estimators, semantic_entropy `strip().lower()`, architecture.md gap, chart.js SRI).
- Resolved: none of the prior P1/P2 findings.
- New: P3 doc-stale finding on `docs/architecture.md:190` — `lub.domains` is no longer empty (now ships `lub.domains.banking` lazy-alias skeleton). Item 4 paths shift from `{compliance,cnh}` (2/8, 1/3) to `{compliance,cnh}` (3/8, 1/3) — `compliance` improved by one test but is still below threshold.

## Tool issues

- Bash mount returned a truncated snapshot of `src/lub/agents/adapters/langgraph.py` (read as ending at line 92, missing the closing `)` on line 94); `check_integrity.py` and `ast.parse` flagged the truncated bytes as `SyntaxError: unterminated string literal at line 93`. Read-tool view confirms the file is intact (95 lines, properly terminated). Per heuristic, NOT flagged as a code finding.

---

*Slack one-liner:* 1 P1 + 3 P2 still open; new P3 — `docs/architecture.md:190` calls `lub.domains` "empty" but pass-33 shipped `lub.domains.banking` skeleton; `compliance` test coverage ticked up to 3/8 (still under).
