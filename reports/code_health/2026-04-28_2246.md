# Code Health Scan — 2026-04-28 22:46 local

**Status:** 🔴 REPAIR NEEDED

**Summary:** 2 P1, 0 P2, 0 P3 findings — disk integrity failed (gating step). Steps 2–7 skipped per spec. **One new flag** this cycle: `src/lub/compliance/frameworks/sr_11_7.py` joins the still-broken `langgraph.py`. Tenth consecutive REPAIR NEEDED cycle.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited **1**. **Two** files flagged (one new vs. the 2216 / 2116 / 2017 / 1916 / 1816 reports):

```
src/lub/agents/adapters/langgraph.py
  - SyntaxError: unterminated string literal (detected at line 93) at line 93
src/lub/compliance/frameworks/sr_11_7.py
  - SyntaxError: unterminated string literal (detected at line 42) at line 42
```

### 1a. `langgraph.py` — unchanged from 2216

Working-tree state: size **3,157 B**, mtime **2026-04-28 16:39:38 UTC**, lines **92**, null bytes **0** — byte-for-byte identical to the 2216 / 2116 / 2017 / 1916 / 1816 snapshots. Last bytes (xxd):

```
0c40: 6973 2061 2073 6361 6666 6f6c 642e 204c  is a scaffold. L
0c50: 616e 6473 20                             ands
```

File ends mid-token: `"LangGraphCompiler.build is a scaffold. Lands ` — no closing `"`, no `)`, no trailing newline. Plain truncation, not the null-byte variant.

**HEAD `610e305` `langgraph.py` is also truncated** (re-verified this cycle: `git show HEAD:…/langgraph.py | python -c "import ast; ast.parse(...)"` raises `SyntaxError`; HEAD copy is 2,723 B and ends mid-string at `"DEFER (v"`). HEAD has shipped a broken file across all ten REPAIR NEEDED cycles.

- **P1** | `src/lub/agents/adapters/langgraph.py:93` | Working-tree file ends mid-string (`"LangGraphCompiler.build is a scaffold. Lands `); HEAD's copy is also truncated (ends mid-string at `"DEFER (v"`) so a `git checkout` cannot recover it. | From a non-bash-mount shell (PowerShell + VS Code/Notepad++), restore the file to the canonical 91-line scaffold (close the `"`, the `)`, and the function body), `Remove-Item C:\code\eb2niw\.git\index.lock`, then `git add` + `git commit` so HEAD no longer carries the truncation. Do **not** repair from this scan (READ-ONLY).

### 1b. `sr_11_7.py` — NEW this cycle

Working-tree state: size **1,528 B**, mtime **2026-04-28 01:43:33 UTC**, lines **41**, null bytes **0**. Last bytes (xxd):

```
05e0: 0a0a 0a5f 5f61 6c6c 5f5f 203d 205b 2243  ...__all__ = ["C
05f0: 524f 5353 5741 4c4b                      ROSSWALK
```

File ends mid-string at `__all__ = ["CROSSWALK` — no closing `"`, no `]`, no newline. Same truncation pattern as `langgraph.py`.

**HEAD `610e305` `sr_11_7.py` is healthy** (1,528 B, AST parses, ends with the full `__all__ = ["CROSSWALK_KEY", "PILLARS", "REGIME", "TITLE", "get_controls"]\n`). The breakage is **working-tree-only**. `diff HEAD…working` confirms the only edit is in the `get_controls` docstring (lines 35–37): Rodrigo updated the wording from `Six of the seven framework modules still declare the looser …` → `All seven framework modules now share this tight return type (RESOLVED 2026-04-28; previously six of seven …)`. The save round-trip dropped the trailing `_KEY", "PILLARS", "REGIME", "TITLE", "get_controls"]\n` from the `__all__` line — the bash-mount truncation pattern striking again, this time on a different file.

Why it didn't appear in the 2216 report despite mtime 01:43:33 (i.e. predating that scan by ~21 h): the bash-mount serves inconsistent bytes for files in this corruption state. Earlier scans evidently read a non-truncated copy from cache; this scan read the truncated one. Treat this as confirmation that the bash-mount is *currently* unreliable for this working tree.

- **P1** | `src/lub/compliance/frameworks/sr_11_7.py:42` | Working-tree file ends mid-string (`__all__ = ["CROSSWALK`); HEAD copy is intact, so the docstring edit is the only real-content delta and the truncation is a save-round-trip artifact. | From a non-bash-mount shell (PowerShell): `git -C C:\code\eb2niw checkout -- 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/sr_11_7.py` to discard the corrupt working copy, then re-apply the docstring edit in VS Code/Notepad++ and save once. Do **not** repair from this scan.

### 1c. Recent git activity

`git log --since="24 hours ago" --oneline` → empty. HEAD remains `610e305 16_PP`. Last five commits unchanged: `610e305 16_PP / b1fbe37 klk / 756aee8 docs(ESTRUTURA_ARQUIVOS)… / df517b2 chore: reorganize root… / f91faf0 dfas`. `git status --short` shows ~hundreds of unrelated `M` lines across `00_Meta/`, `02_Evidencias_Profissionais/`, `.claude/`, etc. — none in `06_Projeto_GitHub/llm-uncertainty-banking/src/lub/`, confirming the two truncated files are the only `lub/` working-tree deltas.

## 2. AST sweep + Python 3.11+ usage

Skipped — gating step failed.

## 3. Architectural drift

Skipped — gating step failed. (HEAD log captured in §1c; no commits in the last 24 h.)

## 4. Code-smell + duplication

Skipped — gating step failed.

## 5. Backend capability + estimator coverage

Skipped — gating step failed.

## 6. Test coverage by subpackage

Skipped — gating step failed.

## 7. Recent-activity recheck (last 24h)

Skipped — gating step failed. The only working-tree changes inside `src/lub/` are the two flagged files in §1, both already covered as P1.

## Top 5 next moves

1. **Restore `sr_11_7.py` from HEAD** (the cheap one — HEAD is intact). From PowerShell: `git -C C:\code\eb2niw checkout -- 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/sr_11_7.py`, then re-apply the docstring update in VS Code and save once. **(new this cycle)**
2. **Repair `langgraph.py` from a non-bash-mount shell** (PowerShell + VS Code/Notepad++) so the truncation cannot survive the round-trip. Restore the closing `"`, `)`, and function body to match the canonical scaffold (`raise NotImplementedError("LangGraphCompiler.build is a scaffold. Lands in v0.3.")`). HEAD is also broken, so a `git checkout` will not help — the canonical bytes must be re-typed. **(unchanged from 1616, 1716, 1816, 1916, 2017, 2116, 2216)**
3. **Commit both repairs immediately** so HEAD `610e305` no longer ships the truncated `langgraph.py` (and so the just-saved `sr_11_7.py` docstring edit reaches HEAD): `Remove-Item C:\code\eb2niw\.git\index.lock; git -C C:\code\eb2niw add 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/compliance/frameworks/sr_11_7.py; git -C C:\code\eb2niw commit -m "fix(lub): restore truncated langgraph.py and sr_11_7.py"`. **(updated from 1716 / 1816 / 1916 / 2017 / 2116 / 2216)**
4. **Verify the repair held** by re-running `python scripts/check_integrity.py` from the same shell that wrote the files *and* from this bash-mount sandbox — if the two disagree, the bash-mount is actively corrupting reads (this cycle is the smoking gun: `sr_11_7.py` flipped from "passes integrity" to "fails integrity" with no on-disk write). **(unchanged from 1816, 1916, 2017, 2116, 2216)**
5. **Investigate `b1fbe37 "klk"`** (`git show b1fbe37 --stat`) for any other paths in that commit that may have been silently truncated alongside `langgraph.py`; given that `sr_11_7.py` just surfaced as a fresh truncation, assume the bash-mount can corrupt any `lub/` file and audit the seven `compliance/frameworks/*.py` siblings as a batch. **(updated from 1716 / 1816 / 1916 / 2017 / 2116 / 2216)**

---

*Compared to previous report (`2026-04-28_2216.md`):*
- **Same:** P1 on `langgraph.py` (3,157 B / mtime 16:39:38 / 92 lines / 0 null bytes / HEAD also truncated); HEAD `610e305`; no commits in the last 24 h; identical Top 5 items 2 and 4.
- **Resolved:** none.
- **New:** **P1 on `src/lub/compliance/frameworks/sr_11_7.py`** — working-tree truncation at `__all__ = ["CROSSWALK` (line 42); HEAD copy is intact, so the fix is `git checkout` + re-apply the one docstring edit. This is the first time a second file has been flagged in this scan series and it is direct evidence that the bash-mount is currently serving inconsistent bytes (mtime predates the previous nine clean reports). Top 5 reordered: the cheap HEAD-checkout repair for `sr_11_7.py` is now item 1; the expensive re-typing of `langgraph.py` slips to item 2. Items 3 and 5 broadened to cover both files.

---

*Net: tenth consecutive 🔴 cycle — `langgraph.py` still truncated in HEAD and a second file (`sr_11_7.py`, working-tree only) just surfaced, confirming the bash-mount is unreliable; repair both from a Windows-side editor and commit before the next scan.*
