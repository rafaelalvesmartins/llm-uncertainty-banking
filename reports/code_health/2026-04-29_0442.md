# Code Health Scan -- 2026-04-29 04:42 UTC

**Status:** 🟠 RECOMMENDED

**Summary:** 1 P1, 3 P2, 4 P3 findings — see "Top 5 next moves".

---

## 1. Disk integrity

`scripts/check_integrity.py` exits 0 (gating step passes). The script reports three "FAIL" entries (`agents/adapters/crewai.py`, `agents/adapters/langgraph.py`, `compliance/frameworks/sr_11_7.py`) but cross-checking via the Read tool shows all three files are intact and parseable end-to-end. This is the documented Windows-mount sync-lag artifact, NOT a code finding (see "Tool issues" below).

## 2. AST sweep + Python 3.11+ usage

No genuine AST parse failures. `tomllib` is imported in 3 modules (`cli/benchmark.py`, `reports/crosswalk.py`, `reports/mapping.py`); these block 3.10 sandbox testing but `pyproject.toml` declares `python_requires>=3.11` so this is intentional. No findings.

## 3. Architectural drift

Five subpackages absent from `docs/architecture.md`: **`challenge`**, **`cnh`**, **`governance`**, **`integrations`**, **`mcp`**. (`dashboard` is referenced obliquely in §"On naming" line 248 — borderline, not flagged.)

The four import-linter contracts are present in `pyproject.toml` (lines 145, 163, 181, 196): layered architecture, trust-layer forbidden, orchestration forbidden, governance forbidden. No drift.

`git log --since="24 hours ago"` returned no commits. `git status --short` shows uncommitted modifications in repo root and many sibling dirs (~hundreds of paths under `00_Meta/`, `02_Evidencias_Profissionais/`); none under `src/lub` itself for *this* repo's working tree (those are sibling paths in the parent eb2niw checkout). No commit performed.

- **P2** | `docs/architecture.md` | five subpackages (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) are not described in the layered-architecture overview | add a short paragraph per subpackage placing each in the L1–L5 layers (or the trust/orchestration crosscut).

## 4. Code-smell + duplication

- **P2** | `src/lub/uncertainty/semantic_entropy.py:91` | inline `a.strip().lower() == b.strip().lower()` duplicates `lub._text_utils.normalize` | replace with `normalize(a) == normalize(b)`.

The three `except Exception` sites flagged by the regex (`ledger/metrics.py:256`, `ledger/store.py:164`, `mcp/tools/challenge.py:294`) all either re-raise after cleanup or fall back deliberately — none is a silent failure. No findings there.

All 18 `_conn` accesses outside `lub.ledger` carry explicit `# noqa: SLF001` markers (challenge, dashboard subpackages); the encapsulation leak is acknowledged and out-of-scope per the Karpathy "surgical" rule.

No bare `except BaseException`, no SQL via f-string / `.format()` / `%` in `execute()`, no inline entropy computations outside `_math_utils.py`.

Long units (>100 lines) are 15 in total; all but two are class definitions where length is structural (e.g., `UncertaintyGuard`=328L, `Ledger`=229L, `Estimator`=143L base class). Two long *functions* worth tracking:

- **P3** | `src/lub/benchmarks/runner.py:113` | `BenchmarkRunner.run` is 124 lines | factor the per-row inner loop into a private `_run_one_row` helper.
- **P3** | `src/lub/reports/assessment.py:330` | `build_assessment_results` is 121 lines | extract the per-finding aggregation into a helper.

`__all__` audit: the five files flagged in the previous report (`cli/__init__.py`, `mcp/tools/challenge.py`, `mcp/tools/context_autopilot.py`, `orchestration/hooks.py`, `reports/assessment.py`) **all now declare `__all__`** (lines 88, 371, 243, 210, 478 respectively). RESOLVED.

TODO/FIXME inventory (12 mentions, 5 files): `agents/adapters/langgraph.py` (2), `dashboard/render.py` (1), `agents/{reporter,policies,core}.py` (1 each), and the two scaffold raises in `agents/adapters/{crewai,langgraph}.py` (each 1). No new TODO findings beyond the chart.js item below.

- **P3** | `src/lub/dashboard/render.py:88` | the supply-chain TODO to pin `chart.js@4.4.1` with SRI is still open; the `<script>` at line 92 loads from jsdelivr without `integrity=` | follow the in-comment recipe and add the SHA-384 hash.

## 5. Backend capability + estimator coverage

Backend `CAPABILITIES` declarations (verified):

| backend | CAPABILITIES |
|---|---|
| `dummy` | GENERATE \| LOGPROBS \| EMBED |
| `hf` | GENERATE \| LOGPROBS \| EMBED |
| `vllm` | GENERATE \| LOGPROBS |
| `openai` | GENERATE \| EMBED |
| `anthropic` | GENERATE |

Estimators with explicit `REQUIRES_CAPABILITIES`: `adaptive_conformal`, `conformal`, `conformal_sampling`, `mondrian_conformal` (LOGPROBS); `graph_laplacian`, `mahalanobis` (EMBED). Base class default = `GENERATE`.

Six estimators consume `gen.logprobs` directly but inherit the base default of `GENERATE` only:

- **P1** | `src/lub/uncertainty/{perplexity,token_logprob,p_true,semantic_entropy,sar,sentence_sar}.py` | each reads `gen.logprobs`/calls `backend.logprobs()` but declares no `REQUIRES_CAPABILITIES` override, so pairing with `openai`/`anthropic` fails late with `AttributeError` instead of at pipeline construction | add `REQUIRES_CAPABILITIES = BackendCapability.GENERATE \| BackendCapability.LOGPROBS` to each (one line per file).

`monte_carlo_dropout` only calls `.generate()` per the grep; the dropout requirement is enforced via duck-typing on the HF backend rather than a capability flag. Not flagged (out of scope for this finding's pattern).

## 6. Test coverage by subpackage

Source-file-to-test ratio (excluding `__init__` and `_private`):

| subpackage | ratio | flag |
|---|---|---|
| `uncertainty` | 21/23 (91%) | OK |
| `calibration` | 6/8 (75%) | OK |
| `orchestration` | 4/7 (57%) | OK |
| `governance` | 2/4 (50%) | OK |
| `reports` | 8/16 (50%) | OK |
| `dashboard` | 2/7 (28%) | low |
| `runtime` | 1/4 (25%) | low |
| `agents` | 2/9 (22%) | low |
| `ledger` | 1/5 (20%) | low |
| `cli` | 1/6 (16%) | low |
| `benchmarks` | 2/12 (16%) | low |
| `wrappers` | 1/7 (14%) | low |
| `compliance` | 1/8 (12%) | low |
| `challenge` | 1/10 (10%) | low |
| `evidence` | 1/3 (33%) | low |
| `cnh` | 0/3 (0%) | none |
| `integrations` | 0/2 (0%) | none |
| `mcp` | 0/8 (0%) | none |

- **P2** | `tests/unit/` | three subpackages have **zero** unit tests (`cnh`, `integrations`, `mcp`) | add at least a smoke-import test per module to detect import-time failures.
- **P3** | `tests/unit/` | nine more subpackages sit below 50% file-to-test coverage (worst: `challenge` 10%, `compliance` 12%, `wrappers` 14%) | track in backlog; not a single-fix item.

## 7. Recent-activity recheck (last 24h)

~30 files modified in the last 24h (mostly `agents/`, `cli/`, `compliance/frameworks/`, `mcp/`, `reports/`, `orchestration/`). Re-running `check_integrity.py` and AST parse via the Read tool on these files: all parse cleanly. The three "FAIL" entries from the integrity script are the same sync-lag artifact noted in §1.

## Tool issues

- Bash mount returned truncated tail bytes for `agents/adapters/crewai.py`, `agents/adapters/langgraph.py`, and `compliance/frameworks/sr_11_7.py` (each ~80 chars short of the file end), making bash-side `python -c "ast.parse(...)"` fail with "unterminated string literal". The Read tool shows all three files complete and syntactically valid. Treated as a documented Windows-mount sync-lag artifact, not a code finding.

## Top 5 next moves

1. **Declare `REQUIRES_CAPABILITIES` on the 6 LOGPROBS-dependent estimators** (`perplexity`, `token_logprob`, `p_true`, `semantic_entropy`, `sar`, `sentence_sar`) so `openai`/`anthropic` backends fail fast at pipeline construction. Highest ROI. **(unchanged)**
2. **Replace `a.strip().lower() == b.strip().lower()` at `src/lub/uncertainty/semantic_entropy.py:91`** with `normalize(a) == normalize(b)` from `lub._text_utils`. One-line change. **(unchanged)**
3. **Document the 5 missing subpackages** (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) in `docs/architecture.md`. **(unchanged)**
4. **Add smoke-import tests for the three zero-coverage subpackages** (`cnh`, `integrations`, `mcp`) — one `import lub.<sub>` test per module catches import-time regressions cheaply. **(new — replaces the resolved `__all__` backfill item)**
5. **Pin `chart.js@4.4.1` with SRI in `dashboard/render.py:88`** per the in-file recipe. **(unchanged)**

---

*Compared to previous report (`2026-04-29_0144.md`):*
- Same: items 1, 2, 3, 5 above (REQUIRES_CAPABILITIES, semantic_entropy strip().lower(), architecture.md gap, chart.js SRI).
- Resolved: previous item 4 — `__all__` is now declared in `cli/__init__.py:88`, `mcp/tools/challenge.py:371`, `mcp/tools/context_autopilot.py:243`, `orchestration/hooks.py:210`, `reports/assessment.py:478`.
- New: zero-coverage subpackages finding (`cnh`, `integrations`, `mcp` have no `tests/unit/test_<file>.py` matches at all).

---

*Slack one-liner:* 1 P1 + 3 P2 open; previous `__all__` backfill is resolved; new finding is zero unit-test coverage in `cnh`, `integrations`, `mcp`.
