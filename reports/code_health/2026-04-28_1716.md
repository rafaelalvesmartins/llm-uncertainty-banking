# Code Health Scan — 2026-04-28 17:16 local

**Status:** 🟠 RECOMMENDED

**Summary:** 1 P1, 4 P2, 3 P3 findings — see "Top 5 next moves". Disk integrity flipped from 🔴 REPAIR NEEDED → 🟢 OK after `langgraph.py` was repaired in the working tree. Steps 2–7 ran for the first time in 30 cycles.

---

## 1. Disk integrity

`scripts/check_integrity.py` exited **0** after scanning **343** `.py` files across 2 roots — **first clean cycle in 30 runs**. `langgraph.py` mtime jumped from `2026-04-27 17:00:27` (29 cycles stuck) to `2026-04-28 16:39:38`; size grew 2,804 → 3,157 bytes; line count 81 → 91. The file now ends with a properly closed `LangGraphCompiler.build` raising `NotImplementedError` (text repaired and extended beyond the prior `90e281b` canonical).

**HEAD unchanged:** still `610e305 16_PP` (commit time `2026-04-27 19:23 -0300`). 24h `git log` window still shows two commits (`610e305 16_PP`, `b1fbe37 klk`) — the repair landed in the **working tree only**, not in HEAD. `git diff HEAD` confirms `langgraph.py` has +10/-1 lines pending.

**`.git/index.lock` still present** — `0 bytes`, mtime `Apr 28 11:17:17 UTC` — byte- and mtime-identical to the prior eight cycles. Eighth consecutive cycle the lock has not moved; nothing live holds it.

**Working-tree drift:** `git status --short 06_Projeto_GitHub/llm-uncertainty-banking/` returns 126 modified paths (was 124 at 1616; +2). 47 of those are under `src/lub/` (was 46; +1, accounted for by the `langgraph.py` repair).

- **P1** | `src/lub/agents/adapters/langgraph.py:1` | Working-tree repair fixes the truncation but HEAD `610e305` still ships the corrupt 81-line/2,723-byte version; any fresh `git checkout -- …/langgraph.py` will re-corrupt | Commit the fix from a non-bash-mount shell after deleting `.git/index.lock`: `git add 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py && git commit -m "fix(lub): restore langgraph.py truncated DEFER block"`.

## 2. AST sweep + Python 3.11+ usage

343 .py files in `src/lub/` + `tests/` — **0 AST failures** (first clean AST sweep visible in this report series; prior cycles were gated by step 1).

**Python 3.11+ usage (would block a 3.10 sandbox):** 6 files import `datetime.UTC` (`calibration/drift.py:30`, `mcp/server.py:199`, `reports/giskard_report.py:32`, `reports/oscal_common.py:19`, `reports/renderer.py:23`, `types.py:14`); 5 files import `tomllib` (`cli/benchmark.py:8`, `reports/crosswalk.py:33`, `reports/dashboard.py:221` w/ `# type: ignore`, `reports/dashboard_sources.py:59` w/ guard, `reports/mapping.py:26`). No `typing.Self` usage. Consistent with declared `python_requires = ">=3.11"`.

## 3. Architectural drift

**Top-level `src/lub/*.py` not explicitly named in `docs/architecture.md`:** `_text_utils.py`, `config.py`, `protocols.py`, `types.py`. (`cli.py`, `guard.py`, `policies.py`, `rails.py`, `telemetry.py` are mentioned in the trust/orchestration contracts in `pyproject.toml` and most are referenced obliquely in the doc.) **Subpackages not mentioned:** `cnh/`, `challenge/`, `dashboard/`, `integrations/`, `mcp/`. None look misplaced; doc is just behind.

**Import-linter contracts:** all four present and intact in `pyproject.toml:142-209` — layered (L1→L5), trust-layer forbidden (rails/policies/guard/telemetry), orchestration forbidden, governance forbidden.

**24h git window:** `610e305 16_PP`, `b1fbe37 klk` — same two commits as 1616. `b1fbe37` is still the commit that introduced the `langgraph.py` truncation (per prior reports).

- **P3** | `docs/architecture.md` | 4 top-level modules (`config.py`, `protocols.py`, `types.py`, `_text_utils.py`) and 5 subpackages (`cnh`, `challenge`, `dashboard`, `integrations`, `mcp`) lack a one-line entry in the layer table | Add a "Cross-cutting modules" sub-section listing each with its layer membership.

## 4. Code-smell + duplication

- **P2** | `src/lub/dashboard/ledger_source.py:72,93,113,152` | 4 direct `self._conn.execute(...)` calls reach into `Ledger`'s private connection — encapsulation leak past `LedgerSummary` protocol | Add typed query helpers (`Ledger.fetch_run_summary`, `Ledger.fetch_run_metrics`, `Ledger.list_runs`) and call those instead.
- **P3** | `src/lub/uncertainty/semantic_entropy.py:91` | `.strip().lower()` on raw text outside `_text_utils.py` | Replace with `lub._text_utils.normalize(...)` so equivalence-checking lives in one place.
- **NICE** | `src/lub/dashboard/render.py:88` | Only actionable TODO in `src/lub` — pin chart.js@4.4.1 with SRI hash | Generate the hash and pin once supply-chain freeze is decided.

**Other anti-patterns scanned, all clean:** inline `entropy = -sum(p * math.log(p)` outside `_math_utils.py` (none); `except Exception: pass` / `except BaseException` (none); SQL via f-string / `.format()` / `%` / `+` in `.execute()` (none across `src/lub/`); `__all__` exports referencing undefined names (only the lazy-loaded `benchmarks/__init__.py` and `uncertainty/__init__.py`, both intentional via `__getattr__`).

**Function/class length:** 26 definitions exceed 100 lines. Top offenders: `guard.UncertaintyGuard` (329), `ledger.store.Ledger` (230), `challenge.replay.ReplayEngine` (186), `benchmarks.runner.BenchmarkRunner` (179), `pipeline.UncertaintyPipeline` (154). All are central API surfaces — splitting them is a meaningful refactor, not cosmetic.

- **P3** | `src/lub/guard.py:120` | `UncertaintyGuard` is 329 lines — single largest class in `src/lub/` | Extract per-policy evaluation methods into a `_PolicyEvaluator` mixin or sibling module.

## 5. Backend capability + estimator coverage

Backends declare `CAPABILITIES`: `Anthropic` = GENERATE; `OpenAI` = GENERATE|EMBED; `HF` = GENERATE|LOGPROBS|EMBED; `vLLM` = GENERATE|LOGPROBS; `Dummy` = GENERATE|LOGPROBS|EMBED. Estimators declare `REQUIRES_CAPABILITIES`: conformal family (Conformal, AdaptiveConformal, ConformalSampling, MondrianConformal) all need GENERATE|LOGPROBS; Mahalanobis and GraphLaplacian need GENERATE|EMBED. The base `Estimator.REQUIRES_CAPABILITIES = GENERATE` matches the base `ModelBackend.CAPABILITIES = GENERATE`.

No (estimator, backend) pair is universally unsatisfiable — every estimator's requirement set is non-empty in at least one backend. Restrictions like "conformal does not work with Anthropic/OpenAI" are intentional fail-loud guards, per `docs/architecture.md` capability section. **No findings.**

## 6. Test coverage by subpackage

Source-file vs `tests/unit/test_<basename>.py` mapping:

- **<50% (flagged):** `cnh` 0/3, `domains` 0/0, `integrations` 0/2, `mcp` 0/8, `challenge` 1/10, `compliance` 1/8, `wrappers` 1/7, `benchmarks` 2/12, `cli` 1/6, `agents` 2/9, `runtime` 1/4, `dashboard` 2/7, `evidence` 1/3, `ledger` 1/5.
- **≥50%:** `governance` 2/4 (50%), `reports` 8/16 (50%), `orchestration` 4/7 (57%), `calibration` 6/8 (75%), `uncertainty` 21/23 (91%).

The two L1 (wrappers) and L4 (benchmarks) layers are the lowest-covered core layers — every estimator and benchmark depends on them.

- **P2** | `src/lub/wrappers/` | 1/7 test files (14%) — only L1 layer with backend-specific surface (`hf.py`, `openai.py`, `anthropic.py`, `vllm.py`, `dummy.py`, `api_base.py`) under-tested | Add at least a smoke `test_<backend>.py` per backend asserting `CAPABILITIES` flag values and the `Generation` contract.
- **P2** | `src/lub/benchmarks/` | 2/12 test files (17%) — L4 datasets (`finqa`, `convfinqa`, `tatqa`, `br_regulatory`, `credit_scoring`, `financial_sentiment`, `_hf_local`) lack unit tests | Add fixture-based `test_<dataset>.py` exercising `__iter__` and `content_hash` on a 1-example slice.
- **P2** | `src/lub/mcp/` | 0/8 test files — entire MCP tool surface has no unit tests, including the recently-edited `mcp/schemas.py` and `mcp/tools/ruflo_compat.py` | Add at least `test_schemas.py` (round-trip validate) and `test_ruflo_compat.py` (capability translation).

## 7. Recent-activity recheck (last 24h)

34 files under `src/lub/`+`tests/` modified since 2026-04-27 17:16. `check_integrity.py` exits 0 over the full tree (already covers these). AST parse on every recent file: **0 failures**. SQL-injection / capability-mismatch regex sweep over the recent set: **0 hits**. Top recent edits: `langgraph.py` (16:39, the repair), `uncertainty/p_true.py` (16:37), `agents/policies.py` (15:34), `agents/core.py` (14:34), `agents/adapters/autogen.py` (13:33), `agents/reporter.py` (13:33), `mcp/schemas.py` (12:30), `cli/report.py` (12:29), `cli/benchmark.py` (12:29), `orchestration/protocols.py` (11:51).

## Tool issues

- `.git/index.lock` (0 bytes, mtime `Apr 28 11:17 UTC`) still blocks `git add`/`commit`. **Eighth consecutive cycle** with byte- and mtime-identical lock — no live git process holds it. Remove from a non-bash-mount shell: `Remove-Item C:\code\eb2niw\.git\index.lock`.
- Working-tree drift in `06_Projeto_GitHub/llm-uncertainty-banking/`: 124 → 126 modified paths (+2; the `langgraph.py` repair plus 1 outside `src/lub/`).

## Top 5 next moves

1. **Commit the working-tree `langgraph.py` repair** to overwrite the corruption in HEAD `610e305`. Without this, any `git checkout --` on the file re-introduces the truncation. Recipe (PowerShell): `Remove-Item C:\code\eb2niw\.git\index.lock; git -C C:\code\eb2niw add 06_Projeto_GitHub/llm-uncertainty-banking/src/lub/agents/adapters/langgraph.py; git -C C:\code\eb2niw commit -m "fix(lub): restore langgraph.py DEFER block"`.
2. **Remove `.git/index.lock`** (eighth cycle stale; blocks #1).
3. **Add wrappers + benchmarks unit tests** (P2 #1, #2 in §6) — the two lowest-covered core layers gate every downstream estimator and report.
4. **Refactor `dashboard/ledger_source.py`** to drop the 4 direct `_conn.execute` calls in favour of `Ledger` query helpers (encapsulation; P2 §4).
5. **Investigate `b1fbe37 "klk"`** (`git show b1fbe37 --stat`) to understand which other ~30 paths in that commit might have been silently truncated alongside `langgraph.py`. **(unchanged from 1616)**

---

*Compared to previous report (`2026-04-28_1616.md`):*
- **Resolved:** disk integrity (🔴 → 🟢); `langgraph.py` repaired in the working tree (29-cycle streak ended); steps 2–7 actually executed for the first time in this report series.
- **Same:** HEAD `610e305` (still corrupt at the commit level — the repair is uncommitted); `.git/index.lock` mtime `Apr 28 11:17 UTC` (8th cycle); 24h commit window steady at 2 (`610e305`, `b1fbe37`); `.py` count steady at 343.
- **New:** working-tree drift 124 → 126 modified paths (+2, of which +1 is the `langgraph.py` repair under `src/lub/` 46→47); steps 4–6 newly visible findings (encapsulation in `dashboard/ledger_source.py`, test gaps in `wrappers`/`benchmarks`/`mcp`, semantic_entropy `.strip().lower()`, doc drift on top-level modules); 26 functions/classes >100 lines surfaced.

---

*Net: disk integrity finally green after 29 cycles, but the fix lives only in the working tree — commit it before the next `checkout --` re-corrupts HEAD's copy.*
