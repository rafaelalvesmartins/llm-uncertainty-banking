# Code Health Scan -- 2026-04-30 06:42 local

**Status:** 🟠 RECOMMENDED

**Summary:** 1 P1, 3 P2, 2 P3 findings — see "Top 5 next moves".

---

## 1. Disk integrity

`scripts/check_integrity.py` exits 0 (gating step passes). The script reports one FAIL on `src/lub/agents/adapters/langgraph.py` ("unterminated string literal at line 93"). Cross-checked via the Read tool: the file is intact end-to-end (95 lines, multi-line `NotImplementedError` argument terminated cleanly, proper EOF at line 95). This is the documented Windows-mount sync-lag artifact, NOT a code finding (see "Tool issues"). No real integrity failures.

## 2. AST sweep + Python 3.11+ usage

No genuine AST parse failures. `tomllib` remains imported in `cli/benchmark.py`, `reports/crosswalk.py`, `reports/mapping.py`; `pyproject.toml` declares `requires-python>=3.11`, so this is intentional. No findings.

## 3. Architectural drift

Five subpackages remain absent from `docs/architecture.md`: **`challenge`**, **`cnh`**, **`governance`**, **`integrations`**, **`mcp`**. The only mention of "MCP" in `docs/architecture.md` (lines 271–272) refers to ruflo's auto-generated MCP surface, not `lub.mcp`. The four import-linter contracts in `pyproject.toml` are all present and unchanged (lines 145, 163, 181, 196: layered-architecture, trust-layer, orchestration-layer, governance-layer). `git log --since="24 hours ago" --oneline` returned no commits. `git status --short` shows 303 modified paths in the parent worktree (`00_Meta/`, `02_Evidencias_Profissionais/`, `.claude/`, plus a number of `src/lub/**/__init__.py` and CLI files). None of the working-tree modifications introduce new `src/lub/<sub>/` subpackages, so the architecture-drift inventory below is unchanged from the prior run. No commit performed.

- **P2** | `docs/architecture.md` | five subpackages (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) are not described in the layered overview | add a short paragraph per subpackage placing each in L1–L5 (or trust/orchestration crosscut). **(unchanged)**

## 4. Code-smell + duplication

- **P2** | `src/lub/uncertainty/semantic_entropy.py:91` | `a.strip().lower() == b.strip().lower()` duplicates the canonical normalizer outside `_text_utils.py` | replace with `normalize(a) == normalize(b)` from `lub._text_utils`. **(unchanged)**

Duplication searches clean: no inline `entropy = -sum(p * math.log(p)` outside `_math_utils.py`; no `except Exception: pass`; no bare `except BaseException`; no f-string / %-format / `.format()` SQL injection patterns; the three direct `_conn` accesses in `challenge/context_autopilot/{ejection,monitor}.py` and `challenge/meta_calibration.py` are all annotated `# noqa: SLF001` (first-party extension, acceptable). Six `TODO`/`FIXME`/`XXX` comments across five files (`agents/adapters/langgraph.py`, `agents/{core,policies,reporter}.py`, `dashboard/render.py`); none rise to a finding on their own.

- **P3** | `src/lub/dashboard/render.py:88` | chart.js loaded from CDN with `crossorigin` but no SRI hash (line 92 `<script src="…/chart.js@4.4.1/…">` has no `integrity=`); in-file TODO at lines 88–91 documents the recipe | run the recipe and add `integrity="sha384-…"` to the `<script>` tag. **(unchanged)**

## 5. Backend capability + estimator coverage

Backends (`src/lub/wrappers/*.py`) declare:
- `dummy`, `hf`: GENERATE | LOGPROBS | EMBED
- `vllm`: GENERATE | LOGPROBS
- `openai`: GENERATE | EMBED (no LOGPROBS)
- `anthropic`: GENERATE only

Estimators with `REQUIRES_CAPABILITIES`: `adaptive_conformal`, `conformal`, `conformal_sampling` (LOGPROBS), `graph_laplacian`, `mahalanobis` (EMBED), `mondrian_conformal` (LOGPROBS).

- **P1** | `src/lub/uncertainty/{perplexity,token_logprob,p_true,semantic_entropy,sar,sentence_sar}.py` | six estimators that consume token-level logprobs do not declare `REQUIRES_CAPABILITIES`, so they inherit the base default (GENERATE only) and silently fall through on `openai`/`anthropic` instead of failing fast at pipeline construction | add `REQUIRES_CAPABILITIES = BackendCapability.GENERATE | BackendCapability.LOGPROBS` to each class. **(unchanged)**

## 6. Test coverage by subpackage

By "tests importing the subpackage" count (more accurate than file-name match):

| Subpackage | src files | importing tests | ratio | status |
|---|---:|---:|---:|---|
| cnh | 3 | 1 | 33% | under |
| compliance | 8 | 2 | 25% | under |
| integrations | 2 | 1 | 50% | at threshold (passes) |
| dashboard | 7 | 5 | 71% | resolved |
| mcp | 9 | 8 | 89% | resolved |
| challenge | 10 | 11 | 110% | resolved |
| governance | 4 | 3 | 75% | resolved |

Movement since `2026-04-29_0932.md`:
- `dashboard` jumped from 0/7 (0%) to 5/7 (71%) — **resolved**.
- `integrations` rose from 0/2 to 1/2 (50%) — **resolved** (at threshold; rule is `< 50%` flag).
- `cnh` rose from 0/3 to 1/3 (33%) — still under, but now 1 importer exists.
- `compliance` rose from 1/8 (12%) to 2/8 (25%) — still under threshold.

- **P2** | `tests/unit/{cnh,compliance}` | two subpackages remain below the 50% importing-tests threshold (`cnh` 1/3, `compliance` 2/8) | add at minimum a smoke-import test (`from lub.<sub> import …`) per source module; `compliance` is the larger gap (8 source files, 2 importers). **(changed — `dashboard` and `integrations` resolved; `compliance` is the new urgent under-covered subpackage)**

## 7. Recent-activity recheck (last 24h)

No commits in last 24h. Files modified in `src/lub/` since the prior report (mtime newer than `reports/code_health/2026-04-29_0932.md`):

- `src/lub/compliance/frameworks/sr_11_7.py`
- `src/lub/compliance/frameworks/__init__.py`

Both parse cleanly under `ast.parse` and `check_integrity.py`. No new findings. `git status --short` shows additional working-tree modifications (uncommitted) on `agents/`, `benchmarks/base.py`, `calibration/scoring_rules.py`, `cli/*.py`, and several `__init__.py` files; none introduce new top-level subpackages or break AST. Per task constraints, never commit.

## Top 5 next moves

1. **Declare `REQUIRES_CAPABILITIES` on the 6 LOGPROBS-dependent estimators** (`perplexity`, `token_logprob`, `p_true`, `semantic_entropy`, `sar`, `sentence_sar`) so `openai`/`anthropic` backends fail fast at pipeline construction. **(unchanged)**
2. **Replace `a.strip().lower() == b.strip().lower()` at `src/lub/uncertainty/semantic_entropy.py:91`** with `normalize(a) == normalize(b)` from `lub._text_utils`. One-line change. **(unchanged)**
3. **Document the 5 missing subpackages** (`challenge`, `cnh`, `governance`, `integrations`, `mcp`) in `docs/architecture.md`. **(unchanged)**
4. **Add smoke-import tests for `compliance` (2/8) and `cnh` (1/3)** — `dashboard` (now 5/7) and `integrations` (now 1/2) have crossed the 50% threshold and are out of the spotlight. **(changed — `dashboard` and `integrations` resolved; `compliance` is the larger new gap)**
5. **Pin `chart.js@4.4.1` with SRI in `dashboard/render.py:92`** per the in-file recipe at lines 88–91. **(unchanged)**

---

*Compared to previous report (`2026-04-29_0932.md`):*
- Same: items 1, 2, 3, 5 (REQUIRES_CAPABILITIES on 6 estimators, semantic_entropy `strip().lower()`, architecture.md gap, chart.js SRI).
- Resolved: `dashboard` (0/7 → 5/7 = 71%) and `integrations` (0/2 → 1/2 = 50%) crossed the 50% importing-tests threshold; both drop out of the test-coverage finding.
- New: `compliance` (1/8 → 2/8 = 25%) remains below threshold and is now the largest under-covered subpackage; `cnh` (0/3 → 1/3 = 33%) still under threshold. Item 4 paths shift from `{dashboard,cnh,integrations}` to `{compliance,cnh}`.

## Tool issues

- Bash mount returned a truncated snapshot of `src/lub/agents/adapters/langgraph.py` (read as ending at line 92, missing the closing `)` on line 94); `check_integrity.py` and `ast.parse` flagged the truncated bytes as `SyntaxError: unterminated string literal at line 93`. Read-tool view confirms the file is intact (95 lines, properly terminated). Per heuristic, NOT flagged as a code finding.

---

*Slack one-liner:* 1 P1 + 3 P2 still open; `dashboard` (5/7) and `integrations` (1/2) crossed the 50% test-coverage threshold; `compliance` (2/8) is now the largest under-covered subpackage.
