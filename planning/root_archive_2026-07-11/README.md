# Root process archive (2026-07-11)

Internal, point-in-time process artifacts moved off the repo root on 2026-07-11
(planning/38 item repo-visitor#44) so the root listing shows only visitor-facing
files. Nothing here is load-bearing for the library or the demo — these are
dated CI dumps, a commit plan, an arXiv-readiness note, a validation brief, and
the DESIGN_DECISIONS outline/personalization worksheet.

Historical dated reports under `reports/code_health/` and `planning/baselines/`
may still link to the old root paths; those are archived point-in-time records
and were intentionally left unedited. `mkdocs_clean.yml` (a stale duplicate of
`mkdocs.yml`) was deleted in the same pass.
